import {tickSupportVehicles} from './support-vehicles.js';
import {installGuardFeatures} from './guard-features.js';
import {prepareDay,plannedDay,withRandom} from './guard-campaign.js';
import {takeResume,restoreGuard,saveGuard,readGuard,requestResume} from './guard-save.js';
import {beginStationRefill,tickStationRefill,stationRefillStatus,updateStationRefillVisual} from './station-refill.js';
import {createNoria,noriaStatus} from './noria.js';
import {fireResistance} from './fire-status.js';
import {createPolice} from './police.js';
import {tickMaintenance} from './fleet-maintenance.js';
import {daySummary} from './day-summary.js';
import {configureCrew,loadGuardSize,normalizeGuardSize,saveGuardSize} from './crew-config.js';
import {shiftBriefHtml} from './shift-briefing.js';
import {simulationClock} from './simulation-clock.js';
import {nonPlayerStuck} from './nonplayer-recovery.js';
import {nearestRoad} from './roads.js';
import {applyStationComposition} from './station-live.js';
import {createLakeIntake,findLakeSupply} from './lake-supply.js';
import {startVentilation,createVentilation,ventilationBusy} from './ventilation.js';
import {canWalkPatient} from './walking-patient.js';
import {loadComposition,composedFleet} from './station-config.js';
import {openStationMenu} from './station-menu.js';
import {crewPanel} from './crew-identity.js';
import {reportMeans} from './command.js';
import {initExtrication,tickExtrications,extricationStatus} from './extrication.js';
import {createExtricationVisuals} from './extrication-visuals.js';
import {initFoam,foamIncident,toggleFoam,tickFoam,decayFoam,foamPower} from './foam.js';
import {createFoamVisuals} from './foam-visuals.js';
import {initElevator,releaseElevator} from './elevator-rescue.js';
import {findHydrantSupply,connectSupply,validateSupply,SUPPLY_REACH} from './water-supply.js';
import {initAerial,initElevatedRescue,tickAerial,transferAerialFlow,requestAerial,aerialBusy,aerialLinked,aerialReturnError,stopAerialFor,aerialStatus,stopAerial} from './aerial-operations.js';
import {createAerialVisuals} from './aerial-visuals.js';
import {formatRadio} from './radio-voice.js';
import {createBuildingActions,initBuildingActions,requestBuildingAction,buildingActionsBusy} from './building-actions.js';
import {createRoadClearance,cleanupPanel} from './road-clearance.js';
import {applyIncidentTemplate,createPatients} from './incident-catalog.js';
import {incidentProps,fireAnchor} from './incident-props.js';
import {disposeObject} from './dispose.js';
import {installSceneLighting,updateSceneLighting} from './scene-lighting.js';
import {staffLevel,staffBadge} from './staff-status.js';
import {fireBar} from './fire-status.js';
import {supplyCrew} from './supply-crew.js';
import {locateIncident} from './incident-location.js';
import {installRotaryBeacons,updateRotaryBeacons,installAmberEffects,updateAmberEffects,installBlueLedEffects,updateBlueLedEffects} from './rotary-beacons.js';
import {tickNursing,nursingMultiplier} from './nursing.js';
import {initWaterRescue,tickWaterRescue,waterStatus,waterBusy} from './water-rescue.js';
import {rescueBoat,installDiveKit,updateWaterVisual} from './water-models.js';
import {chooseRescue,rescueFireFactor,fireMissionComplete} from './incident-events.js';
import {createTrafficControl,inStation} from './traffic-control.js';
import {stationPath,waypointYaw} from './station-routing.js';
import {planCivilian,finishCivilian} from './civilian-routing.js';
import {createReactiveTraffic} from './reactive-traffic.js';
import {createScenePerimeters} from './scene-perimeter.js';
import {createIncidentAftermath,vehicleRemainsObstacle} from './incident-aftermath.js';
import {placementError,tacticalChoices,tacticalKinds} from './tactical-placement.js';
import {createPlacementMarkers} from './placement-markers.js';
import {victimCountFor} from './sim.js';
import {recoverySpot,recoveryDue,resetRecovery,canManualRecovery,manualRecoverySpot,MANUAL_RECOVERY_DISTANCE} from './traffic-recovery.js';
import {recallButtonState,volunteerStatus,requestVolunteers,tickVolunteers,cancelVolunteer,volunteerPanel,initObjectives,objectivesPanel,objectiveResult} from './reinforcements.js';
import {initIncident,tickCommand,radio,situationWithMeans,commandPanel,radioPanel,reinforcementPanel,PRIORITIES} from './command.js';
import {captain,dressCaptain,setInterventionHelmet} from './models.js';
import {loadProfile,saveProfile,normalizeProfile,playerLabel,escapeHtml,profileForm,PLAYER_VEHICLES,GRADES} from './player-profile.js';
import {canChangePlayerVehicle,syncPlayerVehicle,parkedPlayerVehicles} from './player-vehicle.js';
import {PLAYER_HOME,isHomeTime,boardingAtBase,returnPoint,returnYaw,prepareHomeDeparture,beginHomeRoutine,markHomeDeparture,markHomeArrival,personAtCis} from './player-home.js';
import {createPlayerHome} from './player-home-model.js';
import {createVolunteerTravel} from './volunteer-travel.js';
import {clearMove,clearPlacement,queueLeader,footprint,overlaps} from './vehicle-spacing.js';
import {junctions,automaticSiren} from './automatic-siren.js';
import {assignCrew,releaseCrew,recallCrew,dismissCrew,prepareCrew,duty,dutyLabels} from './crew.js';
import {incidentState,incidentBadge} from './incident-state.js';
import {travelMultiplier,vehiclePace} from './roads.js';
import {AMBER_PATTERNS} from './signalling.js';
import {vehicleConsole,consoleIcon,incidentVehicleActions} from './vehicle-console.js';
import {districts} from './city-layout.js';
import {responseVisuals} from './response-visuals.js';
import {roads,hospitalPoint,block} from './roads.js';
import {HOSPITAL_ENTRY,hospitalArrival,hospitalDeparture} from './hospital-routing.js';
import {tickEquipment,equipmentBusy,equipmentStatus,waterMinutes,initWater,setNozzle,tickWater,stow} from './hydraulics.js';
import {stationLife} from './station-life.js';
import * as T from 'three';
import {OrbitControls} from './vendor/OrbitControls.js';
import {neighborhood,cityDensity} from './real-neighborhood.js';
import {addAmbulanceDoors,addPenetrationLights,vehicle,person,cylinder,box,mat} from './models.js';
import {streetRoute,smoothRoute,snap,remainingRouteDistance} from './route3d.js';
import {createShift,tickShift,requestNextCall,nextCallBlocked,setDensity,continueShift,fleet,SHIFT,time} from './sim.js';
import {createIncidentFx} from './effects.js';
import {batchStatic} from './batching.js';
import {canEngage,capability,engagementError} from './operations.js';
import {reserveParking} from './parking.js';
import {sound,toggleSound,testPager,setVolume,isSoundOn,getVolume,stopPager,testTwoTone,stopSiren,syncSiren,sirenDistanceGain,sirenViewGain,sirenOnce,holdSiren,activateAudio,radioVoice,sirenScheduleScale,syncAmbience} from './audio.js';
window.addEventListener('pointerdown',()=>activateAudio().catch(()=>{}),{once:true});window.addEventListener('keydown',()=>activateAudio().catch(()=>{}),{once:true});
let pressedControl=null;
window.addEventListener('pointerdown',ev=>{pressedControl=ev.target.closest?.('button')||null;},true);
window.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' ')pressedControl=ev.target.closest?.('button')||null;},true);
const releasePressedControl=()=>{setTimeout(()=>{pressedControl=null;},0);};
window.addEventListener('pointerup',releasePressedControl,true);window.addEventListener('keyup',releasePressedControl,true);
window.addEventListener('pointercancel',()=>{pressedControl=null;},true);window.addEventListener('blur',()=>{pressedControl=null;});
const markupCache=new WeakMap();function setMarkup(el,content){if(pressedControl&&el.contains?.(pressedControl))return;if(markupCache.get(el)!==content){const open=[...(el.querySelectorAll?.('details[data-crew][open]')||[])].map(d=>d.dataset.crew);el.innerHTML=content;for(const d of el.querySelectorAll?.('details[data-crew]')||[])if(open.includes(d.dataset.crew))d.open=true;markupCache.set(el,content);}}
let extraFeatures=null;
const resumeGuard=takeResume();
const $=id=>document.getElementById(id),state=createShift(),world=new T.Scene();
state.playerProfile=loadProfile();if(!resumeGuard)prepareDay(state,plannedDay());
world.background=new T.Color('#acbfbc');world.fog=new T.Fog('#acbfbc',390,1000);
let renderer;try{renderer=new T.WebGLRenderer({antialias:true,powerPreference:'high-performance'});}catch(e){$('loading').innerHTML='<h2>La vue 3D ne peut pas démarrer</h2><p>Activez l’accélération graphique du navigateur puis rechargez cette page.</p>';throw e;}
renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.shadowMap.type=T.PCFShadowMap;renderer.toneMapping=T.ACESFilmicToneMapping;renderer.toneMappingExposure=1.15;renderer.outputColorSpace=T.SRGBColorSpace;$('scene').appendChild(renderer.domElement);
const camera=new T.PerspectiveCamera(42,innerWidth/innerHeight,.3,1400);camera.position.set(-20,64,164);
const controls=new OrbitControls(camera,renderer.domElement);controls.target.set(-68,0,88);controls.enableDamping=true;controls.dampingFactor=.065;controls.minDistance=18;controls.maxDistance=700;controls.minPolarAngle=.15;controls.maxPolarAngle=Math.PI*.46;controls.mouseButtons={LEFT:T.MOUSE.PAN,MIDDLE:T.MOUSE.DOLLY,RIGHT:T.MOUSE.ROTATE};controls.touches={ONE:T.TOUCH.PAN,TWO:T.TOUCH.DOLLY_ROTATE};
const hemi=new T.HemisphereLight('#e7eee0','#666d54',2);world.add(hemi);const sun=new T.DirectionalLight('#fff0d3',3.2);sun.position.set(-65,115,50);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);sun.shadow.camera.left=-145;sun.shadow.camera.right=145;sun.shadow.camera.top=145;sun.shadow.camera.bottom=-145;sun.shadow.camera.near=1;sun.shadow.camera.far=350;sun.shadow.normalBias=.12;sun.shadow.bias=-.00025;world.add(sun);
const district=neighborhood(world),engines=(resumeGuard?.specs||composedFleet(fleet,loadComposition(fleet))).map(spec=>{const model=vehicle(world,spec.kind,undefined,{...spec,serviceCar:spec.kind==='VLCG'&&state.playerProfile.vehicle==='car'});model.visible=!spec.mutualAid;model.position.set(spec.home[0],.2,spec.home[1]);model.rotation.y=spec.external?Math.PI:spec.home[0]<-70?Math.PI/2:-Math.PI/2;return{...spec,model,status:'ready',crew:0,call:null,path:null,segment:1,progress:0,workStarted:0,beacons:false};});
batchStatic(district.root,[...district.buildings,district.beach.visitors,...district.roofs,...district.lamps,...district.citizens.map(c=>c.model),...district.traffic.map(c=>c.model),...district.hydrants]);
engines.forEach(e=>{installRotaryBeacons(e.model);installBlueLedEffects(e.model);installAmberEffects(e.model);addPenetrationLights(e.model);addAmbulanceDoors(e.model);installSceneLighting(world,e);});
engines.filter(e=>e.kind==='VPL').forEach(installDiveKit);
const homeLife=createPlayerHome(world);
const lakeIntake=createLakeIntake(world);
const ventilation=createVentilation(world,engines,(c,s,m)=>radio(c,state.minute,s,m,log));
const waterBoats=new Map();
engines.forEach(initWater);engines.forEach(initFoam);const foamVisuals=createFoamVisuals(world),extricationVisuals=createExtricationVisuals(world);engines.forEach(initAerial);const aerialVisuals=createAerialVisuals(world,engines);const updateSupplyCrew=supplyCrew(world,engines);const life=stationLife(world),responseFx=responseVisuals(world,engines),fireEffects=new Map();
applyPlayerProfile(state.playerProfile,false);
const raycaster=new T.Raycaster(),pointer=new T.Vector2(),ground=new T.Plane(new T.Vector3(0,1,0),-.3);const pendingUnits=new Set(),cameraKeys=new Set();let incidentPanelOpen=false;
let vehiclePanelOpen=false;
let garageView=true,selectedCall=1,follow=false,moveMode=false,nightOverride=null,lastNight=null,lastUI=0,last=performance.now(),elapsed=0,toastTimer,routeLine=null;
const markers=new Map(),victims=new Map(),hazards=new Map();
const placementMarkers=createPlacementMarkers(world);let tacticalOpen=null,tacticalOptions=[];
const bannerQueue=[];let activeBanner=null;
function log(message,notify=false){const explicit=message.match(/(?:n°\s*|intervention\s+)(\d+)/i),unit=engines.find(e=>message.startsWith(e.id+' ')),callId=explicit?Number(explicit[1]):unit?.call||unit?.lastCall,entry={at:state.minute,message:unit&&callId&&/prend le départ|vers le CH|arrivé au CH|rentré au CIS|disponible sur route/i.test(message)?'Radio · '+formatRadio(callId,unit.id,message.slice(unit.id.length).replace(/^\s*[—–-]?\s*/,'')):message,urgent:notify,callId};message=entry.message;state.logs.push(entry);if(state.logs.length>180)state.logs.shift();const c=state.calls.find(c=>c.id===callId);if(c){c.events??=[];c.events.push(entry);if(c.events.length>60)c.events.shift();}if(notify&&message.startsWith('Radio'))bannerQueue.unshift(entry);else bannerQueue.push(entry);if(!extraFeatures||extraFeatures.radioAllowed(callId,notify&&message.startsWith('Radio')))radioVoice.enqueue(message,notify&&message.startsWith('Radio'));sound('radio');}

const trackingIcon='<svg class="trackingIcon" viewBox="0 0 20 20" aria-hidden="true"><path d="M10 18s6-6.2 6-11A6 6 0 0 0 4 7c0 4.8 6 11 6 11Z"/><circle cx="10" cy="7" r="2"/></svg>';
function selected(){return engines.find(e=>e.id===state.selected);}
function selectIncident(id){closePlacement();vehiclePanelOpen=false;incidentPanelOpen=true;selectedCall=id;pendingUnits.clear();const c=callNow();if(c){setGarage(false);follow=false;$('follow').classList.remove('active');const target=new T.Vector3(c.target[0],0,c.target[1]);controls.target.copy(target);camera.position.copy(target).add(new T.Vector3(35,48,58));}renderUI();}
function callNow(){return state.calls.find(c=>c.id===selectedCall&&c.status!=='closed');}
function selectEngine(e){if(!e)return;closePlacement();vehiclePanelOpen=true;incidentPanelOpen=false;state.selected=e.id;setGarage(false);follow=true;$('follow').classList.add('active');controls.target.copy(e.model.position);camera.position.copy(e.model.position).add(new T.Vector3(22,27,32));engines.forEach(x=>x.model.userData.ring.visible=false);renderUI();showRoute(e);}
function showRoute(e){if(routeLine){world.remove(routeLine);routeLine.geometry.dispose();routeLine.material.dispose();routeLine=null;}if(!e.path)return;const points=e.path.map(p=>new T.Vector3(p[0],.37,p[1]));routeLine=new T.Line(new T.BufferGeometry().setFromPoints(points),new T.LineDashedMaterial({color:'#e1ce93',dashSize:1.1,gapSize:.7,transparent:true,opacity:.65}));routeLine.computeLineDistances();world.add(routeLine);}
const sirenJunctions=junctions(roads),trafficControl=createTrafficControl(sirenJunctions,roads);
const volunteerTravel=createVolunteerTravel(world,state,{vehicles:allVehicles,advance:advanceTraffic,release:v=>trafficControl.release(v)});
const aftermath=createIncidentAftermath(world);
const perimeters=createScenePerimeters(world,{engines,vehicles:allVehicles,release:v=>trafficControl.release(v)});
const buildingActions=createBuildingActions(world,{engines,emit:(c,sender,message)=>radio(c,state.minute,sender,message,log)});
const roadClearance=createRoadClearance(world,{engines,vehicles:allVehicles,advance:advanceVehicle,release:v=>trafficControl.release(v),emit:(c,sender,message)=>radio(c,state.minute,sender,message,log),perimeter:id=>perimeters.records.get(id)});
const reactiveTraffic=createReactiveTraffic({vehicles:allVehicles,engines,crossings:sirenJunctions,release:v=>trafficControl.release(v),hydrants:district.hydrants,pedestrians:district.citizens,permitted:p=>!perimeters.blockedPoint(p,2)});
function advanceTraffic(v,dt){return reactiveTraffic.advance(v,dt,advanceVehicle);}
const noria=createNoria(world,{engines,hydrants:district.hydrants,obstacles:vehicleObstacles,travel:setRoute,emit:log});
const police=createPolice(world,{advance:advanceVehicle,actors:allVehicles,release:v=>trafficControl.release(v)});
function roadVehicles(){return [...district.traffic,...volunteerTravel.vehicles()];}
function allVehicles(){return [...engines,...police.units,...parkedPlayerVehicles(engines.find(e=>e.kind==='VLCG')),...roadVehicles(),...roadClearance.vehicles(),...[...hazards.values()].map(vehicleRemainsObstacle).filter(Boolean),...aftermath.obstacles()];}
function setRoute(e,target,status){
 e.zoneLighting=false;if(e.zoneLightRig){e.zoneLightRig.extension=0;updateSceneLighting(e,0);}if(!(e.status==='departing'&&trafficControl.stationGranted(e)))trafficControl.release(e);e.controlWaiting=null;e.ladderDeployed=false;e.amber=null;e.siren=false;
 if(['enroute','positioning'].includes(status)&&e.parking)target=e.parking.entry;
 const start=[e.model.position.x,e.model.position.z];
 const departure=e.sceneParking&&Math.hypot(start[0]-e.sceneParking.target[0],start[1]-e.sceneParking.target[1])<3?e.sceneParking:null;
 const roadStart=departure?departure.exit:start;
 const startYaw=departure?.yaw??(!inStation(...start)&&nearestRoad(start).distance<4.6?e.model.rotation.y:undefined);
 if(status==='transport'){
  const approach=stationPath(e,roadStart,HOSPITAL_ENTRY,status,streetRoute,{startYaw});
  const arrival=e.kind==='VLI'?[HOSPITAL_ENTRY,[369,-96],[375,-102],[383,-110],[383,-141]]:hospitalArrival(engines.filter(v=>v.kind==='VSAV').indexOf(e));e.path=[...approach,...arrival.slice(1)];
 }else if(e.status==='hospital'){
  e.path=[...hospitalDeparture(start),...stationPath(e,HOSPITAL_ENTRY,target,status,streetRoute).slice(1)];
 }else e.path=stationPath(e,roadStart,target,status,streetRoute,{startYaw,endYaw:['enroute','positioning'].includes(status)?e.parking?.yaw:undefined});
 if(departure){e.path=[...smoothRoute([start,departure.exit]),...e.path.slice(1)];e.maneuver={center:departure.target.slice(),radius:18};}else e.maneuver=null;
 e.sceneParking=null;
 if(['enroute','positioning'].includes(status)&&e.parking){const end=e.path.pop();e.path.push(...smoothRoute([end,e.parking.approach,e.parking.target]).slice(0));}
 markHomeDeparture(state,e);e.segment=1;e.status=status;e.beacons=status!=='returning';resetRecovery(e);if(e===selected())showRoute(e);
}
function arm(e){if(e.external){e.crew=e.size;return true;}if(e.crew)return true;if(e.dedicated)return assignCrew(state,e);if(state.freeStaff<e.size){log(`${e.id} non armé — effectif insuffisant. Déclenchez les astreintes ou attendez un retour.`,true);return false;}assignCrew(state,e);return true;}
function engageUnits(ids=[...pendingUnits]){const c=callNow(),units=ids.map(id=>engines.find(e=>e.id===id)).filter(Boolean);const error=state.ended?'La garde est terminée.':engagementError(units,c,state.freeStaff,state.roster);if(error){log(error,true);return error;}let stationDeparture=false;for(const e of units){const role=capability(e,c),routinePreparation=e.status==='departing'&&e.commuteDestination;const retasked=e.status==='returning';arm(e);e.parking=reserveParking(e,c,engines,vehicleObstacles());e.tacticalPlacement=null;e.call=c.id;e.lastCall=c.id;e.patientAssigned=false;e.patientProgress=0;e.nursingPatient=null;e.support=role==='firstAid';e.roadRescue=role==='roadRescue';e.command=role==='nursing'||role==='supply'||role==='command'||role==='aerial'||role==='medical'||e.roadRescue;e.workActive=false;e.wasAtHome=e.kind==='VLCG'&&e.atResidence&&(e.status==='ready'||!!routinePreparation);e.wasAtStation=!e.atResidence&&!e.external&&(e.status==='ready'||!!routinePreparation&&e.wasAtStation);e.commuteDestination=null;e.returnTo=null;stationDeparture||=e.wasAtStation||e.wasAtHome;e.alertAt=state.minute;e.departAt=state.minute+(e.external?e.mobilization||1:e.wasAtHome?prepareHomeDeparture(state,e):e.wasAtStation?prepareCrew(state,e,Math.random,engines):0);e.retasked=retasked;e.status='departing';e.path=null;e.beacons=!e.wasAtStation&&!e.wasAtHome&&!e.external;log(`${e.id} engagé sur l’intervention n°${c.id}. ${e.support?'Premiers secours en attendant le VSAV.':e.wasAtHome?'Sélectif au domicile · réveil et préparation.':e.wasAtStation?'Sélectif déclenché.':e.mutualAid?`Mobilisation au ${e.base} : ${e.mobilization} min avant trajet.`:'Réengagement depuis sa position sur route.'}`,true);if(retasked)radio(c,state.minute,'Centre → '+e.id,`Nouveau départ, ${c.name}, ${c.address||'adresse transmise'}. Rejoignez les lieux.`,log);}c.status='active';pendingUnits.clear();if(stationDeparture)sound('selective');moveMode=false;if(units.length)selectEngine(units.at(-1));renderUI();return null;}
function respond(){return engageUnits([selected().id]);}
function startReturn(e){e.escortLead=null;if(aerialBusy(e)||aerialLinked(e,engines))return;if(e.external){e.call=null;e.siren=false;e.parking=null;setRoute(e,e.home,'returning');log(`${e.id} — retour au ${e.base||'CH'}.`);return;}stow(e);e.siren=false;e.sirenHeld=false;e.parking=null;e.returnTo=e.kind==='VLCG'?(e.commuteDestination||(isHomeTime(state.minute)?'home':'cis')):'cis';e.commuteDestination=null;setRoute(e,returnPoint(e),'returning');log(`${e.id} — ${e.returnTo==='home'?'Retour domicile':'Retour CIS'}.`);}
function prepareOnSite(e){stopAerialFor(e,engines);e.zoneLighting=false;if(e.external){startReturn(e);return;}e.status='reconditioning';e.path=null;e.beacons=true;stow(e);e.readyAt=state.minute+6;log(`${e.id} — remise en condition sur les lieux avant remise à disposition.`);}
function cancelPreparation(e){trafficControl.release(e);e.controlWaiting=null;e.call=null;e.path=null;e.status='ready';e.beacons=false;e.siren=false;e.commuteDestination=null;e.boarding=[];releaseCrew(state,e);stow(e);syncPlayerVehicle(e,state.playerProfile);}
function returnEngine(e){if(e.longSupplyTarget){log(`${e.id} — rangez la longue alimentation avant le retour.`,true);return;}if(e.escortVehicle&&e.kind==='VLI'){log(`${e.id} — escorte sanitaire en cours jusqu’au CH.`,true);return;}if(e.ventilationCrew){log(`${e.id} — ventilation en cours, attendez le rangement du matériel.`,true);return;}const aerialError=aerialReturnError(e);if(aerialError){log(aerialError,true);return;}const waterCall=state.calls.find(c=>c.id===e.call);if(waterBusy(waterCall,e)){log(`${e.id} — sauvetage nautique en cours, retour possible après débarquement de la victime.`,true);return;}if(['refilling','maintenance','ready','hospital','transport','reconditioning','doctorTransit','awaitingEscortDeparture'].includes(e.status))return;const onScene=e.status==='scene',old=state.calls.find(c=>c.id===e.call);if(old?.patients){const patient=old.patients.find(p=>p.assignedTo===e.id&&!p.evacuated);if(patient)patient.assignedTo=null;}e.call=null;e.patientAssigned=false;e.patientProgress=0;e.support=false;e.command=false;if(old&&old.status!=='closed'){const left=engines.filter(x=>x.call===old.id);old.stabilized=left.some(x=>x.support&&x.status==='scene');old.status=left.length?'active':'waiting';}if(boardingAtBase(e)){cancelPreparation(e);log(`${e.id} — départ annulé, équipage de nouveau disponible ${e.atResidence?'au domicile':'au CIS'}.`);}else if(onScene)prepareOnSite(e);else startReturn(e);renderUI();}
function onCall(c,restoring=false,seeded=false){
 if(!restoring&&!seeded&&state.dayPlan)return withRandom(c.seed||state.dayPlan.seed+c.id*7919,()=>onCall(c,false,true));
 if(!restoring){
 applyIncidentTemplate(c);
 if(c.inspection)c.fireConfirmed=null;
 initIncident(c);initObjectives(c,state.minute);
 if(['SUAP','AVP'].includes(c.type)){c.victimCount??=victimCountFor(c);c.victimsKnown=false;c.evacuated=0;c.patients??=createPatients(c);}
 const choices=roads.filter(r=>!r.name.includes('(simulation)')&&Math.hypot(r.a[0]-r.b[0],r.a[1]-r.b[1])>20&&(c.requires==='CCF'?r.trail:c.setting==='express'||c.name.includes('voie rapide')?r.express:!r.express&&!r.trail&&r.zone!=='forest'));
 const r=choices[Math.floor(Math.random()*choices.length)];
 c.target=[(r.a[0]+r.b[0])/2,(r.a[1]+r.b[1])/2];c.address=r.name;
 locateIncident(c);initElevator(c);initElevatedRescue(c);initWaterRescue(c);initBuildingActions(c);initExtrication(c);
 const responding=engines.filter(e=>capability(e,c)&&!['VLCG','EPA'].includes(e.kind)),tripMinutes=responding.map(e=>{const from=e.status==='ready'?e.home:[e.model.position.x,e.model.position.z],route=streetRoute(from,c.accessTarget||c.target);return route.slice(1).reduce((sum,p,i)=>sum+Math.hypot(p[0]-route[i][0],p[1]-route[i][1]),0)/(7.5*vehiclePace(e)*1.3)+(e.mutualAid?e.mobilization:0);});
 initObjectives(c,state.minute,Math.min(...tripMinutes,120));
 }
 if(c.type==='INC'){const {anchor,scale}=fireAnchor(c),effect=createIncidentFx(world,anchor,scale,c.requires==='CCF'||['forest','vegetation'].includes(c.scene));effect.setActive(!c.inspection);fireEffects.set(c.id,effect);}
 if(!incidentPanelOpen||!callNow())selectedCall=c.id;
 if(!restoring){extraFeatures?.onNewCall(c);sound('printer');log(`18 / 112 — ${c.name}, ${c.address}.`,true);}
 const marker=new T.Mesh(new T.CylinderGeometry(1.5,1.5,.12,32),new T.MeshBasicMaterial({color:c.type==='INC'?'#ef9754':'#7ed4df',transparent:true,opacity:.65}));
 marker.position.set(c.target[0],(c.site?.height||0)+.5,c.target[1]);world.add(marker);markers.set(c.id,marker);
 if(['AVP','OD','INC'].includes(c.type))hazards.set(c.id,incidentProps(world,c));
 if(c.type==='SUAP'||c.type==='AVP'){const victim=person(world,c.actionPoint?.[0]??c.target[0]+6,c.actionPoint?.[1]??c.target[1]-6,'#976857');victim.rotation.z=Math.PI/2;victim.position.y=.6;victims.set(c.id,{model:victim,responders:[]});}
 renderUI();
}
function updateNauticalVisuals(seconds=0){for(const e of engines.filter(e=>e.kind==='VPL'))e.model.userData.carriedBoat.visible=true;for(const c of state.calls){if(!c.nautical)continue;let record=waterBoats.get(c.id);if(c.status==='closed'){if(record){disposeObject(record.boat);disposeObject(record.diver);waterBoats.delete(c.id);}continue;}if(!record){const boat=rescueBoat(world,true),diver=person(world,0,0,'#202c35');box(diver,.56,.45,.36,'#f39c32',0,1.08,0);cylinder(diver,.14,.14,.6,'#b6c6c9',0,1.08,-.3,8);record={boat,diver};waterBoats.set(c.id,record);}const engine=engines.find(e=>e.id===c.nautical.unitId);updateWaterVisual(c,record.boat,victims.get(c.id)?.model,engine,seconds,camera.position);record.diver.visible=!!c.nautical.unitId&&!c.nautical.done&&c.nautical.phase===2;record.diver.position.set(c.nautical.victim[0]+1,0,c.nautical.victim[1]);record.diver.rotation.z=Math.PI/2;}}
function updateElevatorVictims(){
 for(const c of state.calls){if(!c.elevator?.released||!c.victimCount||c.status==='closed'||victims.has(c.id))continue;
  const p=c.actionPoint||c.target,model=person(world,p[0],p[1],'#976857');model.rotation.z=Math.PI/2;model.position.y=.6;victims.set(c.id,{model,responders:[]});
 }
}
function updateRescueVictims(){for(const c of state.calls){if(!c.complication||c.status==='closed')continue;const patient=c.patients.find(p=>p.fromComplication);let record=victims.get(c.id);if(!record){const site=block.buildings.slice().sort((a,b)=>Math.hypot(a.x-c.fireTarget[0],a.z-c.fireTarget[1])-Math.hypot(b.x-c.fireTarget[0],b.z-c.fireTarget[1]))[0],upper=new T.Vector3(site?.x??c.fireTarget[0],Math.min(3.7,(site?.levels||2)*3-2),site?site.z+site.d/2+.6:c.fireTarget[1]+4),lower=new T.Vector3(c.target[0]+4,0,c.target[1]-5);record={model:person(world,0,0,'#ba835c'),responders:[],upper,lower};victims.set(c.id,record);}const ambulance=engines.find(e=>e.id===patient?.assignedTo);record.model.visible=!patient?.evacuated&&!(ambulance?.patientProgress>.75);record.model.position.copy(record.upper).lerp(record.lower,c.complication.progress);}}
function arrive(e){trafficControl.release(e);e.controlWaiting=null;e.reversing=false;e.path=null;e.beacons=false;if(e.status==='returning'){e.status=e.external&&e.doctorAway?'doctorTransit':'ready';const refilling=beginStationRefill(e);e.foamReserve=e.foamCapacity;e.foamOn=false;e.foamFlow=0;const destination=returnPoint(e);e.model.position.set(destination[0],.2,destination[1]);e.model.rotation.y=returnYaw(e);if(!refilling)releaseCrew(state,e);markHomeArrival(state,e);e.siren=false;e.sirenHeld=false;e.amber=false;e.call=null;syncPlayerVehicle(e,state.playerProfile);log(refilling?`${e.id} — plein de la citerne au CIS.`:e.external?`${e.id} au ${e.base||'CH'} — disponible.`:e.atResidence?'Chef de centre à domicile — repos, VLCG garée devant la maison et disponible.':`${e.id} rentré au CIS — disponible en remise.`);return;}if(e.status==='moving'){e.status='idle';log(`${e.id} disponible sur route.`);return;}const c=state.calls.find(c=>c.id===e.call);if(!c||c.status==='closed'){returnEngine(e);return;}const repositioned=e.status==='positioning';e.status='scene';e.beacons=true;e.siren=false;e.maneuver=null;e.sceneParking=e.parking;if(e.parking){e.model.position.set(e.parking.target[0],.2,e.parking.target[1]);e.model.rotation.y=e.parking.yaw;}if(e.noria)return;if(repositioned){radio(c,state.minute,e.id,'Position atteinte : '+e.tacticalPlacement+'.',log);if(e===selected())showRoute(e);return;}if(e.roadRescue){c.roadSecured=true;log(`${e.id} — sécurisation du secours routier en appui du VSAV.`,true);}radio(c,state.minute,e.kind==='VLCG'?playerLabel(state.playerProfile):e.id,'Sur les lieux, reconnaissance engagée.',log);c.firstArrival??=state.minute;if(!e.command||e.roadRescue)c.firstUsefulArrival??=state.minute;e.workStarted=state.minute;e.workActive=false;log(`${e.id} arrivé sur les lieux. ${e.support?'Premiers secours engagés, en attente du VSAV.':c.type==='INC'?(c.inspection?'Levée de doute en cours.':'Reconnaissance en cours. Préparez les lances et l’alimentation.'):c.type==='OD'?'Reconnaissance et dégagement en cours.':'Prise en charge de la victime.'}`,true);if(e.support)c.stabilized=!engines.some(x=>x.call===c.id&&x.kind==='VSAV'&&x.status==='scene');if(e.kind==='VSAV')c.stabilized=false;if(c.type!=='INC'&&!e.command)e.workActive=true;
 if(e===selected())showRoute(e);}
function vehicleObstacles(){return allVehicles().filter(v=>v.model?.visible!==false).map(v=>v.model);}
function advanceVehicle(e,dt){
 if(!e.path)return false;
 e.escortWaiting=false;const escort=e.escortVehicle&&e.kind==='VLI'?engines.find(v=>v.id===e.escortVehicle):null;if(escort?.status==='transport'&&e.model.position.distanceTo(escort.model.position)>20&&!e.escortLeadStart&&!sirenJunctions.some(p=>Math.hypot(p[0]-e.model.position.x,p[1]-e.model.position.z)<32||Math.hypot(p[0]-escort.model.position.x,p[1]-escort.model.position.z)<32)){e.escortWaiting=true;trafficControl.release(e);e.controlWaiting='Escorte · attend le VSAV';return false;}
 if(e.kind==='VLI'&&e.escortLeadStart&&escort?.path&&remainingRouteDistance([e.model.position.x,e.model.position.z],e.path,e.segment)<remainingRouteDistance([escort.model.position.x,escort.model.position.z],escort.path,escort.segment)-10)e.escortLeadStart=null;
 const lead=e.escortLead&&engines.find(v=>v.id===e.escortLead);if(e.status==='transport'&&lead?.status==='transport'&&(lead.escortPending||lead.escortLeadStart||e.model.position.distanceTo(lead.model.position)<8)){e.escortWaiting=true;trafficControl.release(e);e.controlWaiting='Escorte · départ groupé';return false;}
 const actors=allVehicles(),obstacles=actors.map(v=>v.model),civil=e.status==='traffic',speed=e.service?5.5:e.personal?(e.travelSpeed||5):civil?(e.yielding?3:e.road.express?16:5):e.status==='positioning'?4:7.5*vehiclePace(e)*travelMultiplier(e);const weatherSpeed=state.campaign&&state.conditions?.key==='rain'?.85:1;
 let remaining=dt*speed*weatherSpeed*state.speed/60;e.trafficWaiting=false;e.controlWaiting=null;
 while(remaining>0&&e.segment<e.path.length){
  const p=e.path[e.segment],dx=p[0]-e.model.position.x,dz=p[1]-e.model.position.z,d=Math.hypot(dx,dz);
  if(d<.07){e.segment++;continue;}
  const reversing=p.gear===-1,step=Math.min(d,remaining,.5),x=e.model.position.x+dx/d*step,z=e.model.position.z+dz/d*step,yaw=waypointYaw(e.path,e.segment,dx,dz);
  const wait=perimeters.reason(e,x,z,yaw)||trafficControl.reason(e,x,z);if(wait){e.controlWaiting=wait;return false;}
  if(!reversing&&queueLeader(e,actors,dx,dz,yaw)){e.controlWaiting='Distance de sécurité';return false;}
  if(!clearMove(e.model,x,z,yaw,obstacles)){const leader=actors.find(o=>o!==e&&o.path&&o.controlWaiting&&Math.cos(o.model.rotation.y-yaw)>.7&&Math.hypot(o.model.position.x-x,o.model.position.z-z)<14&&(o.model.position.x-x)*dx+(o.model.position.z-z)*dz>0);if(leader)e.controlWaiting='File de circulation';else e.trafficWaiting=true;return false;}
  e.model.position.x=x;e.model.position.z=z;e.model.rotation.y=yaw;if(e.maneuver&&Math.hypot(x-e.maneuver.center[0],z-e.maneuver.center[1])>e.maneuver.radius+8)e.maneuver=null;e.reversing=reversing;
  e.model.userData.wheels.forEach(w=>w.rotation.x+=(reversing?-step:step)/.56);
  remaining-=step;if(step>=d)e.segment++;
 }return e.segment>=e.path.length;
}
function moveEngine(e,dt){if(!advanceVehicle(e,dt))return;const p=e.status==='returning'?returnPoint(e):e.parking?.target,yaw=e.status==='returning'?returnYaw(e):e.parking?.yaw;if(p&&!clearMove(e.model,p[0],p[1],yaw,vehicleObstacles())){e.trafficWaiting=true;return;}arrive(e);}
function releaseIncidentUnits(c){
 for(const unit of engines.filter(x=>x.call===c.id&&!['transport','hospital'].includes(x.status))){
  unit.call=null;unit.support=false;
  if(boardingAtBase(unit))cancelPreparation(unit);else if(unit.status==='scene')prepareOnSite(unit);else startReturn(unit);
 }
}
function finishCall(c,e){
 if(c.status==='closed')return;
 if(c.victimCount&&e.kind==='VSAV'&&e.status==='scene'){
  const patient=c.patients?.find(p=>p.assignedTo===e.id&&!p.evacuated);
  if(!patient)return;
  patient.evacuated=true;patient.releasedOnSite=patient.transportRequired===false;
  e.patientAssigned=false;e.patientProgress=0;c.evacuated++;
  if(c.type!=='INC')c.progress=c.evacuated/c.victimCount;
  if(patient.releasedOnSite){log(`${e.id} — relevage et bilan terminés, maintien à domicile après régulation.`,true);}
  else{
   patient.transportStartedAt=state.minute;e.parking=null;
   setRoute(e,[hospitalPoint[0],hospitalPoint[1]+engines.filter(x=>x.kind==='VSAV').indexOf(e)*11],'transport');
   log(`${e.id} — une victime à bord, départ vers le CH.`,true);
   const nurse=patient.severe?engines.find(v=>v.kind==='VLI'&&v.call===c.id&&v.status==='scene'&&(!patient.nursingUnit||patient.nursingUnit===v.id)):null;
   if(nurse){nurse.escortVehicle=e.id;nurse.escortPending=true;nurse.status='transport';nurse.path=null;nurse.nursingPatient=null;e.escortLead=nurse.id;radio(c,state.minute,nurse.id,`Escortons ${e.id} vers le CH.`,log);}
  }
  if(c.evacuated<c.victimCount){log(`Intervention n°${c.id} : ${c.victimCount-c.evacuated} victime(s) encore sur place.`,true);return;}
 }
 if(c.type==='INC'){
  if(c.progress>=1&&!c.fireContained){
   c.fireContained=true;startVentilation(c);
   for(const unit of engines.filter(x=>x.call===c.id)){if(unit.capacity){stow(unit);unit.workActive=false;}if(unit.aerial)stopAerial(unit);}
   fireEffects.get(c.id)?.dispose();fireEffects.delete(c.id);
   radio(c,state.minute,e.id,(c.inspection&&c.fireConfirmed===false?'Levée de doute terminée : absence d’incendie.':'Foyer éteint.')+(fireMissionComplete(c)?'':' Mise en sécurité et prise en charge à terminer.'),log);
  }
  if(!fireMissionComplete(c))return;
 }
 if(c.elevator){if(releaseElevator(c))radio(c,state.minute,e.id,c.victimCount?'Personne dégagée de l’ascenseur. Relais VSAV nécessaire.':'Occupants libérés, sans blessé.',log);if(!c.elevator.released)return;}
 if(c.victimCount&&c.evacuated<c.victimCount)return;
 if(buildingActionsBusy(c))return;
 c.siteCompletedAt??=state.minute;
 roadClearance.request(c,hazards.get(c.id),state.minute);
 releaseIncidentUnits(c);
 // Evacuated means aboard the VSAV, not handed over to the hospital yet.
 if(c.patients?.some(p=>p.evacuated&&!p.releasedOnSite&&p.deliveredAt==null)){
  if(c.status!=='transport')log(`Intervention n°${c.id} — opérations sur place terminées, transport et remise au CH en cours.`);
  c.status='transport';return;
 }
 c.closedAt=state.minute;c.status='closed';c.progress=1;state.completed++;
 const result=objectiveResult(c);
 log(`Bilan n°${c.id} — arrivée : ${result.arrival?'objectif atteint':'hors délai'} · prise en charge : ${result.finish?'objectif atteint':'hors délai'}.`,true);
 fireEffects.get(c.id)?.dispose();fireEffects.delete(c.id);
 const marker=markers.get(c.id);if(marker){world.remove(marker);marker.geometry.dispose();marker.material.dispose();markers.delete(c.id);}
 const hazard=hazards.get(c.id),retained=aftermath.capture(c,hazard,state.minute);if(hazard){if(!retained&&!roadClearance.owns(hazard))disposeObject(hazard);hazards.delete(c.id);}
 const v=victims.get(c.id);if(v){disposeObject(v.model);v.responders.forEach(disposeObject);victims.delete(c.id);}
 const transported=c.patients?.some(p=>p.deliveredAt!=null);
 log(`Intervention n°${c.id} terminée. ${transported?'Victime(s) remise(s) au service des urgences.':c.type==='INC'?(c.inspection&&c.fireConfirmed===false?'Absence d’incendie confirmée après contrôle.':'Foyer maîtrisé, reconnaissance finale effectuée.'):c.type==='OD'?'Opération diverse terminée, zone sécurisée.':'Personne assistée à domicile, sans transport.'}`,true);
 const next=state.calls.find(c=>c.status!=='closed');if(next)selectedCall=next.id;
}
function completeHospitalHandover(e){
 if(e.kind==='VLI'&&e.escortVehicle){const partner=engines.find(v=>v.id===e.escortVehicle);if(partner&&['transport','hospital'].includes(partner.status))return;if(partner&&partner.model.position.distanceTo(e.model.position)<=30){e.status='awaitingEscortDeparture';return;}e.escortVehicle=null;e.call=null;startReturn(e);return;}
 const c=state.calls.find(c=>c.id===e.call),patient=c?.patients?.find(p=>p.assignedTo===e.id&&p.evacuated&&!p.releasedOnSite&&p.deliveredAt==null);
 if(patient)patient.deliveredAt=state.minute;
 if(c)finishCall(c,e);
 e.call=null;startReturn(e);
}
function moveTraffic(dt){for(const v of district.traffic)if(v.model.visible!==false&&advanceTraffic(v,dt))finishCivilian(v);}
function updateCityPopulation(){const density=cityDensity(state.minute);for(const p of district.citizens)p.model.visible=p.densityRank<=density;for(const v of district.traffic){const wanted=v.densityRank<=density;if(!wanted&&!v.path){trafficControl.release(v);v.model.visible=false;}else if(wanted&&v.model.visible===false&&v.recoveryCooldown==null){const others=vehicleObstacles();if(clearPlacement(v.model,v.model.position.x,v.model.position.z,v.model.rotation.y,others))v.model.visible=true;}}}
function recoverNonPlayers(dt){for(const v of roadVehicles()){
 if(v.recoveryCooldown!=null){v.recoveryCooldown-=dt;if(v.recoveryCooldown>0)continue;const r=v.road,dx=r.b[0]-r.a[0],dz=r.b[1]-r.a[1],n=Math.hypot(dx,dz),w=r.express?5:2.1,x=r.a[0]+dx*.35-dz/n*w,z=r.a[1]+dz*.35+dx/n*w,yaw=Math.atan2(dx,dz);if(!clearPlacement(v.model,x,z,yaw,vehicleObstacles()))continue;v.model.position.set(x,.15,z);v.model.rotation.y=yaw;v.model.visible=true;v.recoveryCooldown=null;v.progress=.35;continue;}
 if(!nonPlayerStuck(v,dt))continue;trafficControl.release(v);if(v.personal)volunteerTravel.recover(v);else{v.model.visible=false;v.path=null;v.yielding=false;v.recoveryCooldown=15;v.stallWatch=null;}state.trafficRecoveries=(state.trafficRecoveries||0)+1;
 }}
function hardResetEngine(e){if(!engines.includes(e)||!e.path)return false;const old=state.calls.find(c=>c.id===e.call),wasTransport=e.status==='transport';trafficControl.release(e);stopAerialFor(e,engines);noria.stop?.(e);stow(e);e.path=null;e.sceneParking=null;e.beacons=false;e.siren=false;e.sirenHeld=false;e.trafficWaiting=false;e.controlWaiting=null;e.model.visible=true;if(wasTransport){const destination=hospitalArrival(engines.filter(v=>v.kind==='VSAV').indexOf(e)).at(-1);e.model.position.set(destination[0],.2,destination[1]);e.status='hospital';e.hospitalArrivedAt=state.minute;e.transportAt=state.minute+15;log(`${e.id} — blocage persistant : transfert de sécurité vers le CH.`,true);}else{const home=e.home;e.model.position.set(home[0],.2,home[1]);e.model.rotation.y=e.home[0]<-70?Math.PI/2:-Math.PI/2;if(old&&old.status!=='closed'&&e.call){e.status='departing';e.wasAtStation=true;e.departAt=state.minute+.25;e.parking=reserveParking(e,old,engines,vehicleObstacles());log(`${e.id} — blocage persistant : retour de sécurité au CIS et reprise du départ.`,true);}else{e.call=null;e.parking=null;e.status='ready';e.patientAssigned=false;e.workActive=false;e.support=false;e.command=false;releaseCrew(state,e);log(`${e.id} — blocage persistant : retour de sécurité au CIS.`,true);}}resetRecovery(e);return true;}
function unblockTraffic(v,dt,moving){if(v.yielding){resetRecovery(v);return;}if(v.controlWaiting?.startsWith('Escorte')||v.controlWaiting?.includes('passage caserne')){resetRecovery(v);return;}if(!recoveryDue(v,dt,moving))return;if(engines.includes(v)){if((v.blockedSeconds||0)>=75)hardResetEngine(v);return;}const p=v.model.position,obstacles=vehicleObstacles();let spot;
 if(v.path){const finalYaw=v.status==='returning'?undefined:v.parking?.yaw;spot=recoverySpot(v.model,v.path,v.segment,obstacles,finalYaw,(x,z,yaw)=>!trafficControl.reason(v,x,z)&&!perimeters.reason(v,x,z,yaw));if(spot)v.segment=spot.segment;
 }else if(v.road){const r=v.road,next=roads.filter(x=>x!==r&&!x.trail&&!x.name.includes('(simulation)')&&(Math.hypot(x.a[0]-r.b[0],x.a[1]-r.b[1])<.01||Math.hypot(x.b[0]-r.b[0],x.b[1]-r.b[1])<.01));const choices=[r,...next.map(n=>Math.hypot(n.a[0]-r.b[0],n.a[1]-r.b[1])<.01?n:{...n,a:n.b,b:n.a})];for(const road of choices){const dx=road.b[0]-road.a[0],dz=road.b[1]-road.a[1],length=Math.hypot(dx,dz),offset=road.express?5:2,yaw=Math.atan2(dx,dz),begin=road===r?v.progress:0;for(let f=begin+6/length;f<.98;f+=4/length){const x=road.a[0]+dx*f-dz/length*offset,z=road.a[1]+dz*f+dx/length*offset;if(Math.hypot(x-p.x,z-p.z)>45)break;if(clearPlacement(v.model,x,z,yaw,obstacles)){spot={x,z,yaw};v.road=road;v.progress=f;break;}}if(spot)break;}}
 if(!spot)return;p.x=spot.x;p.z=spot.z;v.model.rotation.y=spot.yaw;resetRecovery(v);v.trafficWaiting=false;v.controlWaiting=null;if(engines.includes(v))log(`${v.id} — voie bloquée depuis 20 s : reprise du trajet après recalage automatique.`);}
function repositionEngine(e){
 if(!canManualRecovery(e))return false;
 const finalYaw=e.status==='returning'?returnYaw(e):e.parking?.yaw;
 const spot=manualRecoverySpot(e,vehicleObstacles(),finalYaw,(x,z,yaw)=>{
  const f=footprint(e.model,x,z,yaw);
  return !block.buildings.some(b=>overlaps(f,{x:b.x,z:b.z,yaw:0,length:b.d,width:b.w}));
 });
 if(!spot){log(`${e.id} — aucun emplacement libre dans un rayon de ${MANUAL_RECOVERY_DISTANCE} m.`,true);return false;}
 const distance=Math.round(Math.hypot(spot.x-e.model.position.x,spot.z-e.model.position.z));
 trafficControl.release(e);e.model.position.x=spot.x;e.model.position.z=spot.z;e.model.rotation.y=spot.yaw;e.segment=spot.segment;
 e.reversing=e.path[Math.min(e.segment,e.path.length-1)]?.gear===-1;e.trafficWaiting=false;e.controlWaiting=null;resetRecovery(e);
 trafficControl.update(allVehicles(),state.minute);if(e===selected())showRoute(e);
 log(`${e.id} — replacé de ${distance} m. Destination conservée.`,true);return true;
}
function tickAerialWater(dt){for(const c of state.calls)decayFoam(c,dt*state.speed/60);
 const minutes=dt*state.speed/60;
 for(const e of engines)if(['scene','reconditioning'].includes(e.status)){if(!validateSupply(e,[...district.hydrants,lakeIntake],engines))log(`${e.id} — alimentation interrompue : poteau indisponible. Utilisation de la citerne.`,true);e.rescueCrew=state.calls.some(c=>c.complication?.status==='active'&&c.complication.unitId===e.id)?2:0;tickEquipment(e,minutes);}
 tickAerial(engines,state.calls,minutes,(c,sender,message,urgent)=>radio(c,state.minute,sender,message,log,urgent));
 for(const e of engines){if(!e.capacity)continue;const c=state.calls.find(c=>c.id===e.call);if(e.status==='scene'&&c?.reconComplete&&c.status!=='closed'&&c.progress<1&&((c.type==='INC'&&(!c.inspection||c.fireConfirmed===true)&&!c.fireContained)||(c.scene==='fuel'&&e.foamOn))){tickWater(e,waterMinutes(minutes));if(c.type==='INC')e.workActive=e.flow>0;}else{e.externalFlow=0;e.pumpFlow=0;e.flow=0;if(e.hydrant?.userData?.supplyKind==='lake'&&e.supplyProgress>=1)e.water=Math.min(e.capacity,e.water+1000*waterMinutes(minutes));}tickFoam(e,c,minutes,message=>radio(c,state.minute,e.id,message,log,true));}
 transferAerialFlow(engines);
}
let pendingStationRows=null;
function configureStation(rows){pendingStationRows=rows;return refreshStation();}
function refreshStation(){
 if(!pendingStationRows)return {changed:0,pending:0};
 const result=applyStationComposition(engines,fleet,pendingStationRows,{
 create(spec){const model=vehicle(world,spec.kind,undefined,spec);model.position.set(spec.home[0],.2,spec.home[1]);model.rotation.y=spec.home[0]<-70?Math.PI/2:-Math.PI/2;
 if(!clearPlacement(model,model.position.x,model.position.z,model.rotation.y,vehicleObstacles().filter(m=>!engines.some(e=>e.model===m&&e.home.join(':')===spec.home.join(':'))))){disposeObject(model);return null;}
 const e={...spec,model,status:'ready',crew:0,call:null,path:null,segment:1,progress:0,workStarted:0,beacons:false};installRotaryBeacons(model);installBlueLedEffects(model);installAmberEffects(model);addPenetrationLights(model);addAmbulanceDoors(model);installSceneLighting(world,e);if(e.kind==='VPL')installDiveKit(e);initWater(e);initFoam(e);initAerial(e);return e;},
 remove(e){trafficControl.release(e);responseFx.remove(e);updateSupplyCrew.remove(e);aerialVisuals.remove(e);for(const l of e.zoneLightRig?.lights||[])l.target.removeFromParent();disposeObject(e.model);pendingUnits.delete(e.id);},
 replace(old,e){if(e){responseFx.add(e);updateSupplyCrew.add(e);aerialVisuals.add(e);}if(state.selected===old?.id)state.selected=e?.id||'VLCG';}
 });if(!result.pending)pendingStationRows=null;return result;
}
function tickEngines(dt){tickSupportVehicles(engines);extraFeatures?.tick(dt*state.speed/60);for(const c of state.calls)c.weatherFactor=extraFeatures?.weather()||1;for(const e of engines)tickStationRefill(e,dt*state.speed/60,unit=>{releaseCrew(state,unit);log(`${unit.id} — plein terminé, disponible au CIS.`);});noria.update(state,dt*state.speed/60);tickMaintenance(state,engines,log);if(state.pendingGuardSize!=null)configureCrew(state,state.pendingGuardSize);refreshStation();const routine=beginHomeRoutine(state,engines.find(e=>e.kind==='VLCG'));if(routine)log(routine);volunteerTravel.sync();updateCityPopulation();perimeters.update(state.calls,state.minute);aftermath.update(state.minute,p=>perimeters.blockedPoint(p));const closedRoads=perimeters.blockedRoads();for(const v of district.traffic)if(v.model.visible!==false&&v.recoveryCooldown==null&&!v.path&&!v.yielding)planCivilian(v,roads,Math.random,closedRoads);perimeters.reroute(district.traffic);reactiveTraffic.update(roadVehicles(),dt*state.speed/60);trafficControl.update(allVehicles(),state.minute);tickCommand(state,engines,dt,log);reportMeans(state,engines,log);tickExtrications(state.calls,engines,dt*state.speed/60,(c,s,m,u)=>radio(c,state.minute,s,m,log,u));buildingActions.update(state.calls,state.minute,dt*state.speed/60);ventilation.update(state.calls,dt*state.speed/60,state.minute);tickAerialWater(dt);for(const c of state.calls)if(c.status!=='closed'&&c.siteCompletedAt==null&&c.fireContained&&fireMissionComplete(c)){const unit=engines.find(e=>e.call===c.id&&!(e.kind==='VSAV'&&e.status==='scene'))||{id:'Centre',kind:'VLCG'};finishCall(c,unit);}roadClearance.update(state.minute,dt*state.speed/60,dt);for(const e of engines)unblockTraffic(e,dt,!!e.path);moveTraffic(dt);police.update(state,dt);volunteerTravel.move(dt);recoverNonPlayers(dt);for(const e of engines){if(e.escortPending){const partner=engines.find(v=>v.id===e.escortVehicle);if(partner?.status==='transport'){e.escortPending=false;e.escortLeadStart=state.minute;e.parking=null;setRoute(e,hospitalPoint,'transport');}}if(e.status==='awaitingEscortDeparture'){const escort=engines.find(x=>x.id===e.escortVehicle);if(!escort||escort.model.position.distanceTo(e.model.position)>30){e.escortVehicle=null;e.call=null;startReturn(e);}}if(e.status==='departing'&&state.minute>=e.departAt){if((e.wasAtStation||inStation(e.model.position.x,e.model.position.z))&&!trafficControl.stationGranted(e)){e.controlWaiting='Départ en attente · passage caserne';continue;}if(e.commuteDestination){startReturn(e);}else{const c=state.calls.find(c=>c.id===e.call);if(!c||c.status==='closed'){startReturn(e);continue;}setRoute(e,c.target,'enroute');e.departedAt=state.minute;c.firstDeparture??=state.minute;if(e.retasked){radio(c,state.minute,e.id,'Bien reçu, prenons le départ vers la nouvelle intervention.',log);e.retasked=false;}else log(`${e.id} prend le départ — en route sur les lieux.`);}}if(e.path){if(e.status==='transport'){moveEngineTransport(e,dt);}else moveEngine(e,dt);}if(e.status==='hospital'&&state.minute>=e.transportAt)completeHospitalHandover(e);updateSceneLighting(e,dt*state.speed/60);if(e.status==='reconditioning'&&state.minute>=e.readyAt&&!equipmentBusy(e)&&!(e.zoneLightRig?.extension>0))startReturn(e);if(e.status==='scene'){if(e.noria)continue;const c=state.calls.find(c=>c.id===e.call);if(!c||c.status==='closed')continue;if(!c.reconComplete)continue;if(c.inspection&&c.fireConfirmed!==true){if(c.fireConfirmed===false&&e.capacity){stow(e);e.workActive=true;const inspectors=engines.filter(x=>x.call===c.id&&x.status==='scene'&&x.capacity).length;c.progress=Math.min(1,c.progress+dt*state.speed/60/c.duration/Math.max(1,inspectors));if(c.progress>=1)finishCall(c,e);}continue;}if(extraFeatures?.gates(c,e))continue;if(e.kind==='VLI'){tickNursing(e,c,dt*state.speed/60,message=>radio(c,state.minute,e.id,message,log,true));continue;}if(e.kind==='VPL'){tickWaterRescue(c,e,dt*state.speed/60,message=>radio(c,state.minute,e.id,message,log,true));continue;}if(e.support){c.stabilized=!engines.some(x=>x.call===c.id&&x.kind==='VSAV'&&x.status==='scene');continue;}if(e.command&&!(e.kind==='EPA'&&e.aerial?.mode==='attack'))continue;if(e.kind==='VSAV'&&c.victimCount){if(!e.patientAssigned){const patient=(c.patients[c.priorityPatient]&&!c.patients[c.priorityPatient].evacuated&&!c.patients[c.priorityPatient].assignedTo&&!c.patients[c.priorityPatient].trapped?c.patients[c.priorityPatient]:null)||c.patients.find(p=>!p.evacuated&&!p.assignedTo&&!p.trapped);if(!patient)continue;patient.assignedTo=e.id;e.patientAssigned=true;e.patientTransportRequired=patient.transportRequired;e.patientWalking=canWalkPatient(c,patient);e.patientProgress=0;}const patient=c.patients.find(p=>p.assignedTo===e.id&&!p.evacuated);e.needsDoctor=false;e.patientProgress+=dt*state.speed/60/(c.type==='INC'?24:c.duration)*(c.priority==='rescue'?1.2:1)*nursingMultiplier(patient);if(e.patientProgress>=1)finishCall(c,e);continue;}if(c.type==='INC'&&c.fireContained){if(!buildingActionsBusy(c))finishCall(c,e);continue;}if(e.workActive){const totalFlow=c.type==='INC'?engines.filter(x=>x.call===c.id&&x.status==='scene'&&(x.capacity||x.aerial)).reduce((sum,x)=>sum+(x.flow||0),0):0;const power=c.type==='INC'?(totalFlow>0?Math.min(1.4,totalFlow/500)*e.flow/totalFlow:0)/((c.fireLoad||1)*fireResistance(c))*(c.priority==='contain'?.85:1)*rescueFireFactor(c,e)*foamPower(c)*(e.buildingCrew?Math.max(.35,1-e.buildingCrew/(e.crew||e.size||4)):1):1;const duration=c.duration||60;c.progress=Math.min(1,c.progress+dt*state.speed/60/duration*power*(e.protectNeighbor?.5:1));if(c.progress>=1)finishCall(c,e);}}}}
function moveEngineTransport(e,dt){if(advanceVehicle(e,dt)){e.path=null;e.status='hospital';e.hospitalArrivedAt=state.minute;e.transportAt=state.minute+15;const patient=state.calls.find(c=>c.id===e.call)?.patients?.find(p=>p.assignedTo===e.id&&p.evacuated&&!p.releasedOnSite&&p.deliveredAt==null);if(patient)patient.hospitalAt=state.minute;if(e.escortSamu){const samu=engines.find(x=>x.id===e.escortSamu);samu.doctorAway=false;if(samu.status==='doctorTransit')samu.status='ready';e.escortSamu=null;log('Médecin de retour au CH.',true);}e.beacons=false;e.siren=false;}}
function statusText(e){if(e.status==='refilling')return stationRefillStatus(e);if(e.noria)return noriaStatus(e);if(e.status==='maintenance')return `${e.maintenanceReason} · ${Math.max(1,Math.ceil(e.maintenanceUntil-state.minute))} min`;if(e.kind==='VLI'&&e.escortVehicle)return e.status==='hospital'?'Transmission CH':'Escorte '+e.escortVehicle;if(e.extricationTask!=null&&e.status==='scene')return extricationStatus(state.calls.find(c=>c.id===e.extricationTask));if(aerialStatus(e))return aerialStatus(e);if(e.buildingTask&&e.status==='scene')return e.buildingTask+' en cours';if(['scene','reconditioning'].includes(e.status)&&equipmentStatus(e))return equipmentStatus(e);if(e.kind==='VLI'&&e.status==='scene')return e.nursingPatient!=null?'Soins infirmiers en cours':'Infirmier sur place · en soutien';const incident=state.calls.find(c=>c.id===e.call);if(e.kind==='EPA'&&e.status==='scene'&&incident?.reconComplete&&incident.elevatedRescue&&!incident.elevatedRescue.done)return 'Brancardage à lancer · commande EPA';if(e.kind==='VPL'&&e.status==='scene')return waterStatus(incident);if(e.status==='scene'&&incident?.complication?.status==='active'&&incident.complication.unitId===e.id)return 'Sauvetage · '+(incident.complication.waiting||'en cours');if(e.kind==='VSAV'&&e.status==='scene'&&!e.patientAssigned&&incident?.patients?.some(p=>p.trapped))return 'Attend la mise en sécurité de la victime';if(e.kind==='VSAV'&&e.status==='scene'&&incident?.type==='INC'&&!incident?.victimCount)return 'Soutien sanitaire';if(e.controlWaiting)return e.controlWaiting;if(e.reversing&&e.path)return 'Manœuvre · marche arrière';if(e.mutualAid&&e.status==='departing')return `Renfort mobilisé · départ dans ${Math.max(0,Math.ceil(e.departAt-state.minute))} min`;if(e.mutualAid&&e.status==='returning')return 'Retour au '+e.base;if(e.external&&e.status==='ready')return e.mutualAid?'Disponible · '+e.base:'Disponible au CH';if(e.status==='awaitingEscortDeparture')return 'Attend le départ du VSAV médicalisé';if(e.status==='doctorTransit')return 'Au CH · médecin dans un VSAV';if(e.status==='scene'&&e.needsDoctor)return 'Victime grave · attend SAMU';if(e.trafficWaiting&&e.path)return `⚠ Voie occupée · ${Math.floor(e.blockedSeconds||0)} s`;if(e.kind==='VLCG'){if(e.status==='ready'&&e.atResidence)return 'Disponible à domicile · repos';if(e.status==='ready'&&e.model.userData.playerVehicle==='car')return 'Disponible · parking extérieur';if(e.commuteDestination&&e.status==='departing')return e.commuteDestination==='home'?'Prépare le retour à domicile':'Prépare le départ au CIS';if(e.status==='departing'&&e.wasAtHome)return 'Réveil et préparation · domicile';if(e.status==='returning'&&e.returnTo==='home')return 'Disponible sur route · retour domicile';}return{ready:'Disponible en remise',departing:'Sélectif · préparation',enroute:'En route sur les lieux',scene:e.support?'Premiers secours · attend VSAV':e.workActive?'Intervention en cours':'Reconnaissance sur les lieux',returning:'Retour CIS',moving:'Déplacement sur route',positioning:'Mise en position · '+(e.tacticalPlacement||'sur site'),idle:'Disponible sur route',transport:'Transport CH',hospital:'Arrivé au CH',reconditioning:'Remise en condition sur site'}[e.status]||e.status;}
function victimSummary(c){return c?.victimCount?(c.victimsKnown?`${c.victimCount} victime${c.victimCount>1?'s':''}`:'Nombre de victimes à confirmer sur place'):'';}
function meansByNature(units){return [['Secours à personne',units.filter(e=>['VSAV','VLI'].includes(e.kind))],['Incendie',units.filter(e=>['FPT','VSR','CCF','EPA'].includes(e.kind))],['Autres',units.filter(e=>!['VSAV','VLI','FPT','VSR','CCF','EPA'].includes(e.kind))]];}
function closePlacement(){tacticalOpen=null;tacticalOptions=[];placementMarkers.update([]);}
function openPlacement(e){
 incidentPanelOpen=true;vehiclePanelOpen=false;
 const c=callNow(),error=placementError(e,c);if(error){log(error,true);return;}
 if(tacticalOpen===e.id){closePlacement();renderIncident();return;}
 tacticalOptions=tacticalChoices(e,c,engines,{hydrants:district.hydrants,obstacles:vehicleObstacles()});tacticalOpen=e.id;
 follow=false;$('follow').classList.remove('active');
 if(tacticalOptions.length){const center=tacticalOptions.reduce((p,o)=>[p[0]+o.target[0]/tacticalOptions.length,p[1]+o.target[1]/tacticalOptions.length],[0,0]);controls.target.set(center[0],0,center[1]);camera.position.copy(controls.target).add(new T.Vector3(38,62,65));}
 renderIncident();
}
function choosePlacement(e,index){
 const c=state.calls.find(c=>c.id===e.call),option=tacticalOptions[index],error=placementError(e,c);
 if(error||!option||tacticalOpen!==e.id){if(error)log(error,true);return false;}
 const current=tacticalChoices(e,c,engines,{hydrants:district.hydrants,obstacles:vehicleObstacles()});
 if(!current.some(p=>p.id===option.id&&Math.hypot(p.target[0]-option.target[0],p.target[1]-option.target[1])<.01)){log('Cet emplacement vient d’être occupé. Choisissez une autre position.',true);closePlacement();renderIncident();return false;}
 e.parking=option;e.tacticalPlacement=option.label;
 if(e.status==='scene')setRoute(e,option.entry,'positioning');else if(e.status==='enroute')setRoute(e,option.entry,'enroute');
 radio(c,state.minute,e.id,'Placement demandé : '+option.label+'.',log);closePlacement();renderUI();return true;
}
function placementButton(e,c){
 if(!tacticalKinds.includes(e.kind)||!['departing','enroute','scene'].includes(e.status)||c.waterRescue||c.setting==='beach'||c.siteCompletedAt!=null)return '';
 const error=placementError(e,c);
 return `<button class="placementButton" data-placement="${e.id}" aria-label="Positionner ${e.id}" title="${error||'Choisir une position · facultatif'}" aria-expanded="${tacticalOpen===e.id}" ${error?'disabled':''}>⌖</button>`;
}
function placementChoices(e){
 if(tacticalOpen!==e.id)return '';
 return `<div class="placementChoices" aria-label="Positions pour ${e.id}">${tacticalOptions.length?tacticalOptions.map((p,i)=>`<button data-placement-choice="${i}" data-placement-unit="${e.id}" title="${p.distance} m du lieu d’action"><b>${i+1}</b><span>${p.label}</span></button>`).join(''):'<span>Aucun autre emplacement libre à proximité.</span>'}<button class="closePlacement" data-placement-close aria-label="Fermer les positions" title="Conserver le placement automatique">×</button></div>`;
}
function renderIncident(){const c=callNow(),panel=$('incidentPanel');panel.classList.toggle('hidden',garageView||!incidentPanelOpen||!c);if(tacticalOpen){const unit=engines.find(e=>e.id===tacticalOpen);if(!unit||placementError(unit,c)||garageView||!incidentPanelOpen)closePlacement();}placementMarkers.update(tacticalOpen?tacticalOptions:[]);const mission=c?incidentState(c,engines):null;panel.dataset.state=mission?.key||'waiting';if(!c){panel.classList.add('hidden');panel.innerHTML='<span class="eyebrow">ENGAGEMENT DES MOYENS</span><h2>Aucune intervention sélectionnée</h2><p>Sélectionnez un appel pour voir les lieux et engager les moyens.</p>';return;}
 for(const id of pendingUnits){const e=engines.find(e=>e.id===id);if(!e||!canEngage(e)||!capability(e,c))pendingUnits.delete(id);}
 const engaged=engines.filter(e=>e.call===c.id),groups=meansByNature(engaged),chosen=engines.filter(e=>pendingUnits.has(e.id)),extra=chosen.reduce((n,e)=>n+(e.external||e.dedicated||e.crew?0:e.size),0),error=chosen.length?engagementError(chosen,c,state.freeStaff):null;
 setMarkup(panel,`<div class="incidentHeading"><div><span class="incidentIdentifier">I${String(c.id).padStart(2,'0')}</span><span class="eyebrow"> ${c.type}</span><h2>${c.name}</h2><p>${c.address}</p>${c.victimCount?`<p class="victimSummary">${victimSummary(c)}</p>`:''}${incidentBadge(mission)}<span class="missionDetail">${mission.detail}</span>${c.limitedInfo&&!c.firstArrival?'<p class="limitedInfo">Appel nocturne : circonstances et nombre de victimes à confirmer à la reconnaissance.</p>':''}</div><div class="incidentHeaderActions"><button data-focus title="Voir les lieux" aria-label="Voir les lieux">⌖</button><button data-garage title="Vue de la remise" aria-label="Vue de la remise">▥</button><button data-close-incident aria-label="Fermer la fiche intervention" title="Fermer la fiche intervention">×</button></div></div>${fireBar(c,engines)}${reinforcementPanel(c)}${objectivesPanel(c,state.minute,state.speed)}${commandPanel(c,engines)}${cleanupPanel(c)}${extraFeatures?.panel(c)||''}${c.policeStatus&&c.siteCompletedAt==null&&c.status!=='closed'?`<p class="missionDetail">Police · ${c.policeStatus}</p>`:''}<div class="meansLabel">MOYENS ENGAGÉS <span>${engaged.length}</span></div><div class="assignedMeans">${groups.filter(([,units])=>units.length).map(([title,units])=>`<section class="engagedGroup"><h3>${title}</h3>${units.sort((a,b)=>a.id.localeCompare(b.id,'fr',{numeric:true})).map(e=>`<div class="assignedRow isEngaged ${['transport','hospital'].includes(e.status)?'isTransport':''}"><button data-track="${e.id}" title="Suivre ${e.id}" aria-label="Suivre ${e.id}">${e.id}</button><span>${statusText(e)}${e.path?`<small>Arrivée estimée · ${extraFeatures?.eta(e)||1} min</small>`:''}${e.tacticalPlacement&&e.status!=='positioning'?`<small class="placementLabel">⌖ ${e.tacticalPlacement}</small>`:''}</span>${placementButton(e,c)}</div>${incidentVehicleActions(e,{incident:c,engines,supply:e.capacity&&!e.hydrant?nearestSupply(e):null,lakeSupply:findLakeSupply(e,lakeIntake,engines)})}${crewPanel(e,state)}${placementChoices(e)}`).join('')}</section>`).join('')||'<p>Aucun moyen engagé.</p>'}</div>${c.siteCompletedAt!=null?'':`<div class="meansLabel">MOYENS À ENGAGER <span>${state.freeStaff} personnels disponibles</span></div><div class="availableMeans">${meansByNature(engines.filter(e=>capability(e,c)&&e.call!==c.id)).filter(([,units])=>units.length).map(([title,units])=>`<section class="engagedGroup"><h3>${title}</h3><div class="unitChoices">${units.sort((a,b)=>a.kind.localeCompare(b.kind)||a.id.localeCompare(b.id,'fr',{numeric:true})).map(e=>{const role=capability(e,c),armError=engagementError([e],c,state.freeStaff,state.roster),available=!state.ended&&!armError,reason=e.status==='ready'&&armError?'Non armé · effectif insuffisant':!role?(e.kind==='FPT'&&c.requires==='CCF'?'Feu de végétation : CCF requis':'Emploi non adapté'):!available?statusText(e):statusText(e);return`<button data-choose="${e.id}" class="unitChoice ${e.status==='ready'&&!available?'isUnarmed':''} ${e.call===c.id?'isEngaged':''} ${pendingUnits.has(e.id)?'chosen':''}" ${!role||!available?'disabled':''}><b>${pendingUnits.has(e.id)?'✓ ':''}${e.id}</b><span>${e.call===c.id?'✓ Engagé ici · ':''}${reason}</span><small>${e.mutualAid?'Équipage autonome · '+e.mobilization+' min + trajet':e.external?'Équipe extérieure':e.dedicated?(e.kind==='VLI'?'1 infirmier':'Chef de centre'):e.crew?'Équipage conservé':e.size+' pers.'}${role==='firstAid'?' · premiers secours':''}</small></button>`;}).join('')}</div></section>`).join('')}</div>`}${radioPanel(c)}`);
}
function destinationText(e){if(e.kind==='VLCG'&&((e.status==='ready'&&e.atResidence)||(e.status==='returning'&&e.returnTo==='home')||e.commuteDestination==='home'))return PLAYER_HOME.address;if(e.commuteDestination==='cis')return 'CIS Valmont';if(e.mutualAid&&['ready','returning'].includes(e.status))return e.base;if(e.external&&['ready','returning','doctorTransit'].includes(e.status))return 'Centre hospitalier · base SAMU';if(e.status==='ready')return e.model.userData.playerVehicle==='car'?'CIS Valmont · parking extérieur':'CIS Valmont · en remise';if(['transport','hospital'].includes(e.status))return 'Centre hospitalier · urgences';if(e.status==='returning')return 'CIS Valmont';const c=state.calls.find(c=>c.id===e.call);if(c)return `Intervention n° ${c.id} · ${c.address}`;return e.status==='reconditioning'?'Sur site · remise en condition':'Aucune destination';}
function choosePlayerVehicle(variant){if(!Object.hasOwn(PLAYER_VEHICLES,variant))return;const result=applyPlayerProfile({...state.playerProfile,vehicle:variant});const e=engines.find(e=>e.kind==='VLCG');selectEngine(e);vehiclePanelOpen=true;if(result.pending)log(PLAYER_VEHICLES[variant]+' sélectionnée pour votre retour au CIS.');renderVehiclePanel();}
function navigationDistance(e){
 const position=[e.model.position.x,e.model.position.z];let path=e.path,segment=e.segment;
 if(!path&&e.status==='departing'){
  const target=e.parking?.entry||(e.commuteDestination==='home'?PLAYER_HOME.parking:e.basePoint||e.home),key=[e.alertAt,...position,...target].join(':');
  if(e.distancePlan?.key!==key){const planned=stationPath(e.commuteDestination?{...e,returnTo:e.commuteDestination}:e,position,target,e.commuteDestination?'returning':'enroute',streetRoute,{endYaw:e.parking?.yaw});if(e.parking)planned.push(e.parking.approach,e.parking.target);e.distancePlan={key,path:planned};}path=e.distancePlan.path;segment=1;
 }
 const meters=remainingRouteDistance(position,path,segment);if(meters==null)return '';
 const label=meters>=1000?(meters/1000).toFixed(1).replace('.',',')+' km':Math.max(0,Math.round(meters/5)*5)+' m';
 return `<span class="gpsDistance" aria-label="Distance restante : ${label}" title="Distance restante par la route">↱ ${label}</span>`;
}
function renderVehiclePanel(){const p=$('vehiclePanel'),e=selected();p.classList.toggle('hidden',!vehiclePanelOpen);if(!vehiclePanelOpen)return;setMarkup(p,`<div class="incidentHeading"><div><span class="eyebrow">PANNEAU DE COMMANDE</span><h2>${e.id}</h2></div><button data-panel-close aria-label="Fermer les commandes">×</button></div>${e.kind==='VLCG'?`<div class="playerConsole"><div><b>${escapeHtml(playerLabel(state.playerProfile))}</b><small>Vous · chef de centre · ${PLAYER_VEHICLES[e.model.userData.playerVehicle]||'Camionnette'}</small>${e.model.userData.playerVehicle!==state.playerProfile.vehicle?`<small class="profilePending">${PLAYER_VEHICLES[state.playerProfile.vehicle]} au retour au CIS</small>`:''}</div><button data-player-profile>✎ Mon profil</button></div><div class="captainVehicles" role="group" aria-label="Véhicule à utiliser">${Object.entries(PLAYER_VEHICLES).map(([id,label])=>`<button data-player-vehicle="${id}" aria-pressed="${state.playerProfile.vehicle===id}" title="${canChangePlayerVehicle(e)?'Utiliser ce véhicule':'Choix appliqué au retour au CIS'}"><b>${label}</b><small>${e.model.userData.playerVehicle===id?'Véhicule utilisé':id==='car'?'Au parking extérieur':'En remise'}</small></button>`).join('')}</div>`:''}<p class="vehicleDestination"><small>DESTINATION</small><b>${destinationText(e)}</b>${navigationDistance(e)}</p>${vehicleConsole(e,{supply:e.capacity&&!e.hydrant?nearestSupply(e):null,lakeSupply:findLakeSupply(e,lakeIntake,engines),incident:state.calls.find(c=>c.id===e.call),engines,boarding:boardingAtBase(e),autoSiren:automaticSiren(e,roadVehicles(),sirenJunctions),follow,returnLabel:e.kind==='VLCG'&&isHomeTime(state.minute)?'Retour au domicile':'Retour au CIS'})}${crewPanel(e,state)}${e.kind==='VLI'?'<p>Soins automatiques aux victimes graves accessibles. Le VSAV reste chargé du transport.</p>':''}`);}
function releaseHeldSiren(){engines.forEach(e=>e.sirenHeld=false);holdSiren(false);}
function applyPlayerProfile(value,persist=true){
 const profile=normalizeProfile(value),cg=engines.find(e=>e.kind==='VLCG');state.playerProfile=profile;
 cg.playerProfile=profile;cg.name=playerLabel(profile)+' · chef de centre';
 const player=state.roster.find(p=>p.role==='captain');if(player)Object.assign(player,{name:profile.name,grade:profile.grade,outfit:profile.outfit});
 syncPlayerVehicle(cg,profile);if(cg.officer)dressCaptain(cg.officer,profile);
 return {saved:!persist||saveProfile(profile),pending:cg.model.userData.playerVehicle!==profile.vehicle};
}
function showPlayerProfile(){
 const wasPaused=state.paused;state.paused=true;cameraKeys.clear();releaseHeldSiren();reinforcementsOpen=false;
 modal(profileForm(state.playerProfile,canChangePlayerVehicle(engines.find(e=>e.kind==='VLCG'))));
 $('modal').addEventListener('close',()=>{state.paused=wasPaused||state.ended;renderUI();},{once:true});
 $('playerName').focus();
 $('playerProfileForm').onsubmit=ev=>{
  ev.preventDefault();const fields=ev.currentTarget.elements,read=name=>fields.namedItem(name).value;
  const result=applyPlayerProfile({name:read('playerName'),grade:read('playerGrade'),outfit:read('playerOutfit'),vehicle:read('playerVehicle')});
  $('modal').close();renderUI();
  if(result.pending)log('Votre véhicule sera changé à votre retour en remise.');
  if(!result.saved)log('Profil appliqué à cette garde. Ce navigateur ne permet pas de conserver vos choix après fermeture.');
 };
 renderUI();
}
// Commande momentanée : relâchement, perte de focus ou annulation coupent le son.
const soundProjection=new T.Vector3();let soundOccluders=[],soundLayoutAt=-Infinity;
function vehicleSirenGain(e){
 if(document.hidden||e.model.visible===false)return 0;
 const now=performance.now();if(now-soundLayoutAt>250){soundLayoutAt=now;soundOccluders=['header','.dispatch','#fleetSidebar','#incidentPanel','#vehiclePanel','.bottom','#modal'].map(selector=>document.querySelector(selector)).filter(el=>el&&!el.classList.contains('hidden')&&!(el.id==='modal'&&!el.open)).map(el=>el.getBoundingClientRect?.()).filter(r=>r?.width&&r?.height);}
 camera.updateMatrixWorld();soundProjection.copy(e.model.position);soundProjection.y+=1.4;soundProjection.project(camera);
 const x=(soundProjection.x+1)*innerWidth/2,y=(1-soundProjection.y)*innerHeight/2,covered=soundOccluders.some(r=>x>=r.left&&x<=r.right&&y>=r.top&&y<=r.bottom);
 return sirenViewGain(camera.position.distanceTo(e.model.position),soundProjection,covered);
}
$('vehiclePanel').addEventListener('pointerdown',ev=>{if(ev.target.closest('[data-siren-once]')){ev.preventDefault();selected().sirenHeld=true;holdSiren(true,vehicleSirenGain(selected())*sirenScheduleScale(state.minute,selected().sirenMode),selected().kind==='SAMU');}});
window.addEventListener('pointerup',()=>{releaseHeldSiren();down=null;});window.addEventListener('pointercancel',()=>releaseHeldSiren());window.addEventListener('blur',()=>releaseHeldSiren());
$('vehiclePanel').addEventListener('keydown',ev=>{if([' ','Enter'].includes(ev.key)&&ev.target.closest('[data-siren-once]')){ev.preventDefault();selected().sirenHeld=true;holdSiren(true,vehicleSirenGain(selected())*sirenScheduleScale(state.minute,selected().sirenMode),selected().kind==='SAMU');}});
window.addEventListener('keyup',ev=>{if([' ','Enter'].includes(ev.key))releaseHeldSiren();});
window.addEventListener('keydown',ev=>{if(ev.code!=='KeyH'||ev.repeat||ev.ctrlKey||ev.metaKey||ev.altKey||ev.target?.closest?.('input,select,textarea,[contenteditable=true]')||$('modal').open||state.composingStation)return;const e=selected();if(!e)return;ev.preventDefault();e.sirenHeld=true;holdSiren(true,vehicleSirenGain(e)*sirenScheduleScale(state.minute,e.sirenMode),e.kind==='SAMU');});
window.addEventListener('keyup',ev=>{if(ev.code==='KeyH')releaseHeldSiren();});
$('vehiclePanel').onclick=ev=>{const choice=ev.target.closest('[data-player-vehicle]');if(choice){choosePlayerVehicle(choice.dataset.playerVehicle);return;}if(ev.target.closest('[data-player-profile]')){showPlayerProfile();return;}if(ev.target.closest('[data-back-incident]')){if(selected().call)selectIncident(selected().call);return;}const e=selected();const sirenMode=ev.target.closest('[data-siren-mode]')?.dataset.sirenMode;if(['auto','day','night'].includes(sirenMode)){e.sirenMode=sirenMode;renderVehiclePanel();return;}if(ev.target.closest('[data-lake-supply]')){toggleLakeSupply(e);renderUI();return;}if(ev.target.closest('[data-foam]')){const c=state.calls.find(c=>c.id===e.call),error=toggleFoam(e,c);if(error)log(error,true);else radio(c,state.minute,e.id,e.foamOn?'Établissement mousse engagé.':'Mousse coupée, lance à l’eau.',log);renderUI();return;}if(ev.target.closest('[data-noria]')){const error=noria.toggle(e,state.calls.find(c=>c.id===e.call));if(error)log(error,true);renderUI();return;}const aerialButton=ev.target.closest('[data-aerial-action]');if(aerialButton){const c=state.calls.find(c=>c.id===e.call),error=requestAerial(e,c,aerialButton.dataset.aerialAction,engines,(sender,message)=>radio(c,state.minute,sender,message,log));if(error)log(error,true);else if(aerialButton.dataset.aerialAction==='rescue'){follow=false;const p=e.model.position,q=c.elevatedRescue.upper;controls.target.set((p.x+q[0])/2,q[1]/2,(p.z+q[2])/2);camera.position.copy(controls.target).add(new T.Vector3(28,22,32));}renderUI();return;}if(ev.target.closest('[data-unblock]')){repositionEngine(e);renderUI();return;}if(ev.target.closest('[data-panel-close]')){releaseHeldSiren();vehiclePanelOpen=false;}else if(ev.target.closest('[data-zone-light]')&&e.zoneLightRig&&e.status==='scene'){e.zoneLighting=!e.zoneLighting;}else if(ev.target.closest('[data-ladder]')&&e.kind==='EPA'&&e.status==='scene'&&(!aerialBusy(e)||e.aerial?.mode==='position')){const c=state.calls.find(c=>c.id===e.call);if(c?.elevatedRescue&&!c.elevatedRescue.done){const error=requestAerial(e,c,'rescue',engines,(sender,message)=>radio(c,state.minute,sender,message,log));if(error)log(error,true);}else {const error=requestAerial(e,c,'position',engines);if(error)log(error,true);}}else if(ev.target.closest('[data-blue]')&&!boardingAtBase(e))e.beacons=!e.beacons;else if(ev.target.closest('[data-amber-mode]')){const mode=ev.target.closest('[data-amber-mode]').dataset.amberMode;if(mode==='off')e.amber=false;else if(AMBER_PATTERNS.some(([id])=>id===mode)){e.amberPattern=mode;e.amber=true;}}else if(ev.target.closest('[data-siren-once]')){}else if(ev.target.closest('[data-siren]')){e.siren=!e.siren;}else if(ev.target.closest('[data-cis]'))returnEngine(e);else if(ev.target.closest('[data-follow]')){follow=true;setGarage(false);follow=true;}else if(ev.target.closest('[data-hydrant]')){toggleHydrant(e);}else{const b=ev.target.closest('[data-nozzle]');if(b)setNozzle(e,b.dataset.nozzle,Number(b.dataset.number));}renderVehiclePanel();};
const supplyChoices=new WeakMap();
function nearestSupply(e){
 const key=[e.status,...e.model.position.toArray(),e.model.rotation.y,e.supplyProgress>0,district.hydrants.map(h=>[h.id,h.visible,!!h.parent,...h.position.toArray()].join(':')).join('/'),engines.map(v=>[v.hydrant?.id,v.supplyProgress>0?v.supplyHydrant?.id:null].join(':')).join('/')].join('|');
 const cached=supplyChoices.get(e);if(cached?.key===key)return cached.value;
 const value=findHydrantSupply(e,district.hydrants,engines);supplyChoices.set(e,{key,value});return value;
}
function toggleLakeSupply(e){
 if(e.hydrant?.userData?.supplyKind==='lake'){e.hydrant=null;log(`${e.id} — rangement des tuyaux d’aspiration.`);return true;}
 if(!connectSupply(e,findLakeSupply(e,lakeIntake,engines))){log(`${e.id} — rejoindre le point d’aspiration du lac, puis replier l’alimentation précédente.`,true);return false;}
 log(`${e.id} — installation des aspiraux et de la crépine au lac.`);return true;
}
function toggleHydrant(e){
 if(e.status!=='scene'||!e.capacity)return false;
 if(e.hydrant){e.hydrant=null;log(`${e.id} — rangement de l’alimentation.`,true);return true;}
 if(e.supplyProgress>0){log(`${e.id} — terminez le rangement de l’alimentation.`,true);return false;}
 const supply=findHydrantSupply(e,district.hydrants,engines);
 if(!connectSupply(e,supply)){log(`${e.id} — aucun poteau accessible avec ${SUPPLY_REACH} m de tuyaux. Utilisation de la citerne.`,true);return false;}
 log(`${e.id} — alimentation sur poteau : ${Math.round(supply.distance)} m de tuyaux à établir.`,true);return true;
}
function renderRadioBanner(){const now=Date.now();if(activeBanner&&now-activeBanner.started>=10000)activeBanner=null;if(!activeBanner&&bannerQueue.length)activeBanner={...bannerQueue.shift(),started:now};const banner=$('radioBanner');banner.hidden=!activeBanner;if(!activeBanner){setMarkup(banner,'');return;}const m=activeBanner;setMarkup(banner,`<div class="radioTicker ${m.urgent?'radioUrgent':''}" role="status">${m.callId?`<button data-radio-call="${m.callId}">📻 ${time(m.at)} · ${escapeHtml(m.message)}</button>`:`📻 ${time(m.at)} · ${escapeHtml(m.message)}`}${bannerQueue.length?`<small> · ${bannerQueue.length} message(s) à suivre</small>`:''}</div>`);}

$('radioBanner').onclick=ev=>{const ack=ev.target.closest('[data-radio-ack]'),open=ev.target.closest('[data-radio-call]');if(ack){const c=state.calls.find(c=>c.id===Number(ack.dataset.radioAck));if(c){c.radioAlert=null;c.reinforcementAlerts=[];}renderUI();}else if(open)selectIncident(Number(open.dataset.radioCall));};
function renderCall(c){
 const mission=incidentState(c,engines),engaged=engines.filter(e=>e.call===c.id);
 return `<button class="call ${c.id===selectedCall?'selected':''}" data-state="${mission.key}" data-call="${c.id}" aria-pressed="${c.id===selectedCall}"><div class="meta"><span class="incidentIdentifier">I${String(c.id).padStart(2,'0')}</span><span class="type">${c.type}</span><time class="callTime">${time(c.at)}</time></div>${incidentBadge(mission)}<h3>${c.name}</h3><p>${c.address}</p>${engaged.length?`<div class="callUnits" aria-label="Secours engagés">${engaged.sort((a,b)=>a.id.localeCompare(b.id,'fr',{numeric:true})).map(e=>`<span class="${['scene','transport','hospital'].includes(e.status)?'onScene':''}" title="${e.id} · ${statusText(e)}">${e.id}</span>`).join('')}</div>`:''}<div class="missionAlerts">${c.reinforcementAlerts?.length?'<span class="pendingReinforcement">⚠ Renfort demandé</span>':''}${c.complication?.status==='decision'?'<span class="pendingReinforcement">⚠ Décision de sauvetage attendue</span>':''}</div>${c.victimCount?`<p>${victimSummary(c)}</p>`:''}<p class="missionDetail">${mission.detail}</p>${c.type==='INC'?fireBar(c,engines,true):c.progress?`<div class="progress"><i style="width:${c.progress*100}%"></i></div>`:''}</button>`;
}
function renderUI(){renderRadioBanner();const e=selected(),c=callNow(),cg=engines.find(x=>x.kind==='VLCG');$('captainName').textContent=playerLabel(state.playerProfile);$('captainCard').title=playerLabel(state.playerProfile)+' · vous · suivre le VLCG';$('captainCard').ariaLabel=$('captainCard').title;$('captainStatus').textContent=statusText(cg);$('captainCard').classList.toggle('trafficBlocked',!!cg.trafficWaiting&&!!cg.path);$('clock').textContent=time(state.minute);$('shiftProgress').style.width=Math.min(100,(state.minute-state.shiftStart)/SHIFT.duration*100)+'%';setMarkup($('daySummary'),daySummary(state));$('received').textContent=state.calls.filter(c=>c.at>=state.shiftStart).length;$('completed').textContent=state.calls.filter(c=>c.closedAt>=state.shiftStart).length;const open=state.calls.filter(c=>c.status!=='closed');$('count').textContent=String(open.length).padStart(2,'0');setMarkup($('callList'),state.incoming.map(c=>`<div class="incomingCall"><b>☎ Appel entrant n° ${c.phoneId}</b><p>Opérateur en ligne…</p><span>Motif en cours de qualification</span></div>`).join('')+open.map(renderCall).join('')||'<div class="empty">Aucun appel en attente.<br>Le centre reste disponible.</div>');
 const atCis=state.roster.filter(p=>personAtCis(p,engines));setMarkup($('staff'),staffBadge(state.freeStaff)+`<span class="staffDetail">${atCis.filter(p=>p.kind==='SPP').length} SPP · ${atCis.filter(p=>p.kind==='SPV').length} SPV ${state.minute%1440>=600&&state.minute%1440<720?'de garde · sport en ville':'au CIS'}<br>Infirmier ${atCis.some(p=>p.role==='nurse'&&!p.engine)?'disponible':'engagé'} · ${GRADES[state.playerProfile.grade]} ${atCis.some(p=>p.role==='captain')?'au CIS':cg.atResidence?'à domicile':cg.call?'engagé':'en trajet'}</span>`);const level=staffLevel(state.freeStaff);if(level>(state.staffWarningLevel||0))log(`${GRADES[state.playerProfile.grade]} — ${level===2?'effectif critique':'effectif réduit'} : ${state.freeStaff} équipiers disponibles. Pensez au rappel SPV.`,true);state.staffWarningLevel=level;$('dismissSpv').disabled=!state.roster.some(p=>p.present&&p.kind==='SPV'&&!p.engine);setMarkup($('fleet'),[['Secours à personne',['VSAV','VLI']],['Incendie · secours routier',['FPT','VSR','CCF']],['Appui',['EPA','VTU','VPL']]].map(([title,kinds])=>`<section class="fleetGroup" style="--rows:${Math.max(1,Math.ceil(engines.filter(x=>kinds.includes(x.kind)&&(!x.mutualAid||x.status!=='ready')).length/2))}"><h3>${title}</h3><div class="fleetGroupCards">${engines.filter(x=>kinds.includes(x.kind)&&(!x.mutualAid||x.status!=='ready')).sort((a,b)=>kinds.indexOf(a.kind)-kinds.indexOf(b.kind)||a.id.localeCompare(b.id,'fr',{numeric:true})).map(x=>{const available=canEngage(x)&&(x.crew||(x.dedicated?state.roster.some(p=>p.present&&!p.engine&&p.role===(x.kind==='VLI'?'nurse':'captain')):state.freeStaff>=x.size));const status=x.status==='ready'&&!available?'Non armé — effectif insuffisant':statusText(x);return`<button data-engine="${x.id}" title="${x.id} · ${status}" class="fleetCard ${['transport','hospital'].includes(x.status)?'isTransport':''} ${x.call&&x.call===selectedCall?'isEngaged':''} ${x.trafficWaiting&&x.path?'trafficBlocked':''} ${available?(x.status==='ready'?'isAvailable':'isReturning'):(x.status==='ready'?'isUnarmed':'isUnavailable')}"><span class="mini">${x.id}</span><span class="fleetInfo">${status}<span class="crew">${x.dedicated?(x.kind==='VLI'?'1 INFIRMIER':'CHEF DE CENTRE'):x.size+' PERSONNELS'}</span></span></button>`;}).join('')}</div></section>`).join(''));$('vehicleIcon').textContent=e.id;$('selectedName').textContent=e.name;$('selectedStatus').textContent=statusText(e);$('crewSize').textContent=e.dedicated?(e.kind==='VLI'?'1 INFIRMIER':'CHEF DE CENTRE'):e.size+' PERS.';$('return').textContent=e.kind==='VLCG'&&isHomeTime(state.minute)?'Retour au domicile':'Retour au CIS';$('return').disabled=['refilling','maintenance','ready','hospital','transport','reconditioning','doctorTransit','awaitingEscortDeparture'].includes(e.status);$('move').disabled=!canEngage(e)||state.ended;$('extinguish').style.display='none';$('commandHint').textContent=moveMode?'Cliquez sur une rue pour y déplacer l’engin.':'Les moyens s’engagent depuis le panneau de l’intervention.';$('pause').textContent=state.ended?'Bilan':state.paused?'▶':'Ⅱ';$('pause').ariaLabel=state.ended?'Afficher le bilan de garde':state.paused?'Reprendre la garde':'Mettre en pause';$('logCount').textContent=state.logs.length;const arriving=(state.recallRequests||[]).filter(r=>r.status==='enroute');$('volunteerArrivals').hidden=!arriving.length;$('volunteerArrivals').textContent=arriving.length?`${arriving.length} SPV en approche · ${arriving.filter(r=>['walking','changing'].includes(r.phase)).length} au parking / vestiaires`:'';for(const [id,level] of [['recallDuty',1],['recallAll',2]]){const b=$(id),r=recallButtonState(state,level);b.disabled=r.disabled;b.title=r.label;b.ariaLabel=r.label;const label=b.querySelector?.('i');if(label)label.textContent=r.label;b.classList.toggle('recallActive',r.active);}$('recall').disabled=state.ended;const reinforcementCount=(state.recallRequests||[]).filter(r=>r.status==='enroute').length;$('recall').title=reinforcementCount?`${reinforcementCount} SPV en route`:'Voir les renforts SPV';$('recall').ariaLabel=$('recall').title;$('nextCall').disabled=!!nextCallBlocked(state);$('nextCall').title=nextCallBlocked(state)||'Avancer le temps jusqu’au prochain appel prévu';const el=$('logList');if(!el._lastEntry||el._lastEntry!==state.logs.at(-1)){const bottom=el.scrollHeight-el.scrollTop-el.clientHeight<40;el.innerHTML=state.logs.slice(-60).map(l=>`<div class="log"><time>${time(l.at)}</time>${escapeHtml(l.message)}</div>`).join('');el._lastEntry=state.logs.at(-1);if(bottom)el.scrollTop=el.scrollHeight;}renderIncident();renderVehiclePanel();if(reinforcementsOpen&&$('modal').open)setMarkup($('recallStatus'),volunteerStatus(state));}
$('incidentPanel').addEventListener('toggle',ev=>{if(ev.target.matches?.('.radioDetails')){const c=callNow();if(c)c.radioExpanded=ev.target.open;}},true);
$('incidentPanel').onchange=ev=>{if(ev.target.matches?.('[data-priority]')){const c=callNow();if(c?.reconComplete&&PRIORITIES[ev.target.value]){c.priority=ev.target.value;radio(c,state.minute,'Vous · capitaine','Ordre : '+PRIORITIES[c.priority],log);renderUI();}}};
$('incidentPanel').onclick=ev=>{const building=ev.target.closest('[data-building-action]');if(building){const c=callNow();if(c){const error=requestBuildingAction(c,building.dataset.buildingAction,engines,state.minute,(sender,message)=>radio(c,state.minute,sender,message,log));if(error)log(error,true);}renderUI();return;}const place=ev.target.closest('[data-placement]'),choice=ev.target.closest('[data-placement-choice]');if(place){const e=engines.find(e=>e.id===place.dataset.placement);if(e)openPlacement(e);return;}if(choice){const e=engines.find(e=>e.id===choice.dataset.placementUnit);if(e)choosePlacement(e,Number(choice.dataset.placementChoice));return;}if(ev.target.closest('[data-placement-close]')){closePlacement();renderIncident();return;}if(ev.target.closest('[data-garage]')){setGarage(true);return;}const rescue=ev.target.closest('[data-rescue]');if(rescue){const c=callNow();const error=chooseRescue(c,rescue.dataset.rescue,engines,state.minute,(sender,msg,urgent)=>radio(c,state.minute,sender,msg,log,urgent));if(error)log(error,true);renderUI();return;}if(ev.target.closest('[data-ack-radio]')){const c=callNow();if(c){c.radioAlert=null;c.reinforcementAlerts=[];}renderUI();return;}if(ev.target.closest('[data-captain-go]')){engageUnits(['VLCG']);return;}if(ev.target.closest('[data-radio-report]')){const c=callNow();if(c&&state.minute>=(c.nextReportAt||0)){c.nextReportAt=state.minute+3;const unit=engines.find(e=>e.call===c.id&&e.status==='scene'),report=unit?situationWithMeans(c,engines):null;if(report)c.meansReport=report.key;radio(c,state.minute,unit?.id||'Centre',report?.message||'Aucun moyen encore sur les lieux.',log);renderUI();}return;}if(ev.target.closest('[data-close-incident]')){incidentPanelOpen=false;renderIncident();return;}const choose=ev.target.closest('[data-choose]'),track=ev.target.closest('[data-track]'),work=ev.target.closest('[data-work]');if(choose){const id=choose.dataset.choose;engageUnits([id]);}else if(track){selectEngine(engines.find(e=>e.id===track.dataset.track));setGarage(false);follow=true;$('follow').classList.add('active');}else if(work){const e=engines.find(e=>e.id===work.dataset.work);if(e&&e.status==='scene'&&e.call===selectedCall){state.selected=e.id;vehiclePanelOpen=true;incidentPanelOpen=false;renderIncident();renderVehiclePanel();const command=work.dataset.command;if(command){const control=$('vehiclePanel').querySelector('[data-'+command+(work.dataset.commandValue?'="'+work.dataset.commandValue+'"':'')+']');control?.focus();control?.scrollIntoView({block:'nearest'});}}}else if(ev.target.closest('[data-confirm]'))engageUnits();else if(ev.target.closest('[data-focus]'))selectIncident(selectedCall);};
$('editPlayerProfile').onclick=()=>showPlayerProfile();
$('briefShift').onclick=()=>showShiftBrief();
$('captainCard').onclick=()=>{selectEngine(engines.find(e=>e.kind==='VLCG'));vehiclePanelOpen=true;incidentPanelOpen=false;renderUI();};
$('fleet').onclick=ev=>{const b=ev.target.closest('[data-engine]');if(!b)return;const e=engines.find(e=>e.id===b.dataset.engine);selectEngine(e);setGarage(false);follow=true;vehiclePanelOpen=true;incidentPanelOpen=false;controls.target.copy(e.model.position);camera.position.copy(e.model.position).add(new T.Vector3(22,27,32));renderUI();};
$('callList').onclick=ev=>{const b=ev.target.closest('[data-call]');if(b){selectIncident(Number(b.dataset.call));}};$('respond').onclick=respond;$('return').onclick=()=>{returnEngine(selected());renderUI();};$('move').onclick=()=>{moveMode=!moveMode;renderUI();};$('extinguish').onclick=()=>{selected().workActive=true;log('FPT — lance établie. Extinction en cours.',true);renderUI();};$('pause').onclick=()=>{if(state.ended){endShift();return;}state.paused=!state.paused;renderUI();};$('speed').onchange=ev=>state.speed=Number(ev.target.value);$('density').oninput=ev=>{setDensity(state,Number(ev.target.value));$('densityValue').textContent=state.density;};
$('follow').onclick=()=>{setGarage(false);follow=!follow;$('follow').classList.toggle('active',follow);};$('night').onclick=()=>{nightOverride=!lastNight;};renderer.domElement.addEventListener('pointermove',ev=>{if(down&&down.button===0&&Math.hypot(ev.clientX-down.x,ev.clientY-down.y)>6){follow=false;$('follow').classList.remove('active');}});$('collapseCalls').onclick=()=>{document.querySelector('.dispatch').classList.toggle('collapsed');$('collapseCalls').textContent=document.querySelector('.dispatch').classList.contains('collapsed')?'+':'−';};$('journalToggle').onclick=()=>$('journal').classList.toggle('hidden');$('closeJournal').onclick=()=>$('journal').classList.add('hidden');
function quickRecall(level){const n=requestVolunteers(state,level);if(n)sound('selective');log(`${level===1?'Astreinte':'Rappel général'} : ${n} SPV en route.`,true);renderUI();}
$('recallDuty').onclick=()=>quickRecall(1);$('recallAll').onclick=()=>quickRecall(2);
let reinforcementsOpen=false;function showReinforcements(){reinforcementsOpen=true;modal(volunteerPanel(state));$('modalContent').querySelectorAll('[data-recall-level]').forEach(b=>b.onclick=()=>{const level=Number(b.dataset.recallLevel),n=requestVolunteers(state,level);sound('selective');log(`${level===1?'Rappel astreinte':'Appel général'} : ${n} SPV en route.`,true);showReinforcements();});$('recallStatus').onclick=ev=>{const b=ev.target.closest('[data-cancel-recall]');if(b){cancelVolunteer(state,Number(b.dataset.cancelRecall));showReinforcements();}};}
function modal(html){$('modalContent').innerHTML=html;$('modal').showModal();$('modalContent').querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>{reinforcementsOpen=false;$('modal').close();});}
function openComposer(onBack=null){return openStationMenu(state,fleet,rows=>{pendingStationRows=rows;return refreshStation();},onBack);}
function showShiftBrief(){
 const wasPaused=state.paused;state.paused=true;cameraKeys.clear();releaseHeldSiren();let guardSize=loadGuardSize();
 modal(shiftBriefHtml(state,engines,guardSize));const dialog=$('modal'),feedback=$('modalContent').querySelector('.briefFeedback');
 dialog.addEventListener('close',()=>{state.paused=wasPaused||state.ended;renderUI();},{once:true});
 const adjust=delta=>{guardSize=normalizeGuardSize(guardSize+delta);const error=saveGuardSize(guardSize),applied=!error&&configureCrew(state,guardSize);for(const el of $('modalContent').querySelectorAll('[data-brief-total],[data-brief-count]'))el.textContent=guardSize;$('modalContent').querySelector('[data-brief-operational]').textContent=Math.max(2,guardSize-2);feedback.textContent=error||(applied?`Effectif fixé à ${guardSize}, capitaine et infirmier inclus.`:`Effectif de ${guardSize} enregistré · appliqué au retour des équipes.`);renderUI();};
 $('modalContent').querySelector('[data-brief-minus]').onclick=()=>adjust(-1);$('modalContent').querySelector('[data-brief-plus]').onclick=()=>adjust(1);
 $('modalContent').querySelector('[data-brief-fleet]').onclick=()=>{dialog.close();setTimeout(()=>openComposer(showShiftBrief),0);};
 $('modalContent').querySelector('[data-brief-start]').onclick=()=>{if(!state.dayPlan&&!state.calls.length)prepareDay(state,{seed:Math.floor(Math.random()*4294967296),weather:state.conditions.key,profile:'balanced',mode:'guard'});dialog.close();};const dayOptions=$('modalContent').querySelector('[data-day-options]');if(dayOptions)dayOptions.onclick=()=>{dialog.close();extraFeatures.openDay();};renderUI();
}
function updateAudioButton(){const on=isSoundOn(),button=$('audio');button.classList.toggle('muted',!on);button.setAttribute('aria-pressed',String(!on));button.setAttribute('aria-label',on?'Mettre en sourdine':'Réactiver le son');button.title=button.getAttribute('aria-label');button.querySelector('span').textContent=on?'🔊':'🔇';}
$('audio').onclick=async()=>{await toggleSound();updateAudioButton();};
$('settings').onclick=()=>{modal(`<span class="eyebrow">CONFIGURATION</span><h2>Sons et radio</h2><p>Réglez ici le volume et les essais sonores.</p><label for="volume">Volume sonore</label><input id="volume" type="range" min="0" max="1" step=".05" value="${getVolume()}"><div class="soundTests"><button id="testPager">Tester le bipeur</button><button id="testTwoTone">Tester le deux-tons</button><button id="radioVoice" ${radioVoice.supported?'':'disabled'}>${radioVoice.supported?(radioVoice.isMuted()?'Radio parlée coupée':'Radio parlée active'):'Radio parlée indisponible'}</button><button id="testRadio" ${radioVoice.supported?'':'disabled'}>Tester la radio</button></div><div class="actions"><button data-close>Fermer</button></div>`);$('volume').oninput=e=>setVolume(Number(e.target.value));$('testPager').onclick=async()=>{await testPager();updateAudioButton();};$('testTwoTone').onclick=async()=>{await testTwoTone();updateAudioButton();};$('radioVoice').onclick=()=>{radioVoice.setMuted(!radioVoice.isMuted());radioVoice.activate();$('radioVoice').textContent=radioVoice.isMuted()?'Radio parlée coupée':'Radio parlée active';};$('testRadio').onclick=async()=>{if(!isSoundOn())await toggleSound();radioVoice.setMuted(false);radioVoice.activate();radioVoice.enqueue('Radio · CIS : Essai radio. Centre de secours de Valmont, reçu cinq sur cinq.',true,true);$('radioVoice').textContent='Radio parlée active';updateAudioButton();};};
$('help').onclick=()=>modal(`<span class="eyebrow">UNE GARDE AU CSP</span><h2>Jouer dans le quartier</h2><p>La garde dure 24 heures, de 08 h à 08 h, avec 5 à 20 interventions. À vitesse normale, 24 heures se déroulent en 24 minutes. Le curseur règle la densité de 5 à 20 interventions par garde.</p><ol><li>Sélectionnez une intervention : la caméra se centre sur les lieux.</li><li>Cliquez sur chaque moyen disponible dans son panneau pour le déclencher immédiatement.</li><li>Suivez les moyens engagés et établissez une lance à leur arrivée sur un feu. Le bouton ⌖ propose des positions facultatives, numérotées sur la carte. Repliez le matériel avant de déplacer un engin déjà installé.</li><li>Un engin disponible sur route peut être réengagé immédiatement. La remise en condition se fait sur site ou au CH.</li><li>Le CCF assure les feux urbains, les feux de végétation et les opérations diverses. Le FPT ne traite pas les feux de végétation.</li><li>Le FPTL est un fourgon compact de 2 000 L pour les feux urbains. Pour la végétation, engagez les CCF.</li><li>Un VTU peut assurer les premiers secours en attendant un VSAV. Il ne remplace pas le transport sanitaire.</li></ol><p>Sur un feu de bâtiment, les commandes ↗ Évacuer et ϟ Gaz / élec. deviennent disponibles après reconnaissance. L’équipe rejoint les lieux, accompagne les occupants ou coupe les énergies, puis revient à l’engin. Les deux opérations se succèdent pour garder un équipage disponible. Après un accident, le dépannage et le nettoyage de la chaussée sont automatiques ; les secours peuvent rentrer pendant que la voirie termine.</p><p>Les automobilistes se rangent pour les secours lorsqu’un bas-côté est libre, puis reprennent leur voie après le convoi.</p><p>A/E font tourner la caméra. Q/Z/S/D ou le glissement déplacent la vue. Clic droit et glissement font pivoter la caméra. La molette zoome. La croix ferme la fiche sans annuler les engagements. Cliquez sur une intervention pour la rouvrir.</p><p>Ville fictive : centre-ville, centre commercial, forêt et lac, immeubles, stade et voie rapide. Deux objectifs par mission : premier moyen opérationnel et fin de prise en charge. Les retards n’arrêtent pas l’intervention. Le son est actif dès votre premier clic. Les soins et les opérations diverses démarrent automatiquement après reconnaissance. Sur un feu, choisissez vos lances et l’alimentation dans la commande de l’engin. Le VLI assiste les victimes graves, le VPL assure le sauvetage nautique avec son bateau. Dans le panneau de l’EPA, l’icône lance déclenche une attaque en hauteur après alimentation d’un fourgon proche sur poteau. L’icône brancard lance l’évacuation par nacelle lorsqu’un bilan signale un escalier trop étroit. Le VSAV prend le relais au sol. Attendez la remise de la victime et le repli avant de repartir.</p><p>Le chef de centre rentre dormir à son domicile vers 22 h avec sa VLCG et rejoint le CIS vers 7 h. Une intervention en cours passe en priorité. Il reste engageable depuis sa maison, après réveil et préparation.</p><div class="actions"><button data-close>Reprendre la garde</button></div>`);
$('dismissSpv').onclick=()=>{const n=dismissCrew(state);log(`${n} SPV disponibles quittent le CIS. Les équipages engagés restent mobilisés.`,true);renderUI();};$('recall').onclick=()=>showReinforcements();$('nextCall').onclick=()=>{requestNextCall(state,()=>sound('call'),advanceSimulation);renderUI();};
let down=null;renderer.domElement.addEventListener('pointerdown',e=>down={x:e.clientX,y:e.clientY,button:e.button});renderer.domElement.addEventListener('pointerup',ev=>{if(!down||down.button!==0||Math.hypot(ev.clientX-down.x,ev.clientY-down.y)>6)return;pointer.set(ev.clientX/innerWidth*2-1,-ev.clientY/innerHeight*2+1);raycaster.setFromCamera(pointer,camera);if(tacticalOpen){const hit=raycaster.intersectObjects(placementMarkers.root.children.filter(m=>m.visible),true)[0];if(hit){let node=hit.object;while(node&&node.userData.placementIndex==null)node=node.parent;const e=engines.find(e=>e.id===tacticalOpen);if(node&&e)choosePlacement(e,node.userData.placementIndex);return;}}const hit=raycaster.intersectObjects([...engines.map(e=>e.model),...parkedPlayerVehicles(engines.find(e=>e.kind==='VLCG')).map(v=>v.model)],true)[0];if(hit){const cg=engines.find(e=>e.kind==='VLCG'),spare=parkedPlayerVehicles(cg).find(v=>{let n=hit.object;while(n){if(n===v.model)return true;n=n.parent;}return false;});if(spare){incidentPanelOpen=false;choosePlayerVehicle(spare.model.userData.playerVehicle);renderIncident();return;}const e=engines.find(e=>{let n=hit.object;while(n){if(n===e.model)return true;n=n.parent;}return false;});if(e){selectEngine(e);vehiclePanelOpen=true;incidentPanelOpen=false;renderIncident();renderVehiclePanel();return;}}if(moveMode){const p=new T.Vector3();if(raycaster.ray.intersectPlane(ground,p)){const e=selected();if(arm(e)){setRoute(e,[p.x,p.z],'moving');moveMode=false;log(`${e.id} — déplacement vers le point désigné.`,true);renderUI();}}}});
function setGarage(v){if(v)closePlacement();garageView=v;district.roofs.forEach(r=>r.visible=false);$('garage').classList.toggle('active',v);$('garageLabels').classList.add('hidden');$('incidentPanel').classList.toggle('hidden',v||!incidentPanelOpen||!callNow());if(v){follow=false;$('follow').classList.remove('active');controls.target.set(-70,0,86);camera.position.set(-38,65,147);}}
$('composeStation').onclick=()=>openComposer();
$('garage').onclick=()=>{vehiclePanelOpen=false;incidentPanelOpen=false;renderVehiclePanel();setGarage(true);};
window.addEventListener('keydown',ev=>{if(!['q','z','s','d','a','e'].includes(ev.key.toLowerCase())||ev.ctrlKey||ev.metaKey||ev.altKey||ev.target?.closest?.('input,select,textarea,[contenteditable=true]')||$('modal').open||state.composingStation)return;ev.preventDefault();cameraKeys.add(ev.key.toLowerCase());if(['q','z','s','d'].includes(ev.key.toLowerCase())){follow=false;$('follow').classList.remove('active');}});
window.addEventListener('keyup',ev=>cameraKeys.delete(ev.key.toLowerCase()));window.addEventListener('blur',()=>cameraKeys.clear());
function panKeyboard(dt){if(!cameraKeys.size)return;const turn=(cameraKeys.has('a')?1:0)-(cameraKeys.has('e')?1:0);if(turn){const offset=camera.position.clone().sub(controls.target);offset.applyAxisAngle(new T.Vector3(0,1,0),turn*dt);camera.position.copy(controls.target).add(offset);}const direction=camera.getWorldDirection(new T.Vector3());direction.y=0;direction.normalize();const right=new T.Vector3().crossVectors(direction,new T.Vector3(0,1,0)),movement=new T.Vector3();if(cameraKeys.has('z'))movement.add(direction);if(cameraKeys.has('s'))movement.sub(direction);if(cameraKeys.has('d'))movement.add(right);if(cameraKeys.has('q'))movement.sub(right);movement.normalize().multiplyScalar(dt*Math.max(16,camera.position.distanceTo(controls.target)*.55));controls.target.add(movement);camera.position.add(movement);}
if(typeof ResizeObserver!=='undefined'){
 const header=document.querySelector('header'),footer=document.querySelector('.bottom'),dispatch=document.querySelector('.dispatch'),fleet=$('fleetSidebar'),panels=[$('incidentPanel'),$('vehiclePanel')];
 const layout=new ResizeObserver(()=>{
  const head=header.getBoundingClientRect(),dock=footer.getBoundingClientRect(),style=document.documentElement.style;
  const setSize=(key,value)=>{if(style.getPropertyValue(key)!==value)style.setProperty(key,value);};
  setSize('--hud-top',(head.bottom+16)+'px');setSize('--dock-height',dock.height+'px');
  // Frame the unobstructed map; on smaller screens the detail sheet opens on the left.
  let left=12,right=fleet.getBoundingClientRect().left-12,top=head.bottom+16;
  for(const node of [dispatch,...panels]){
   if(node.classList.contains('hidden')||getComputedStyle(node).visibility==='hidden')continue;
   const rect=node.getBoundingClientRect();if(!rect.width||!rect.height)continue;
   if(rect.left<innerWidth*.1)left=Math.max(left,rect.right+12);else right=Math.min(right,rect.left-12);
  }
  const radio=$('radioBanner');if(!radio.hidden)top=Math.max(top,radio.getBoundingClientRect().bottom+12);
  const bottom=dock.top-12;
  if(right-left>80&&bottom-top>80)camera.setViewOffset(innerWidth,innerHeight,innerWidth/2-(left+right)/2,innerHeight/2-(top+bottom)/2,innerWidth,innerHeight);
  else camera.clearViewOffset();
 });
 for(const node of [header,footer,dispatch,fleet,...panels,$('radioBanner')])layout.observe(node);
}
window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);});
function endShift(){extraFeatures?.rememberResult();state.paused=true;radioVoice.stop();stopPager();stopSiren();releaseHeldSiren();const calls=state.calls.filter(c=>c.at>=state.shiftStart&&c.at<state.shiftEnd),closed=state.calls.filter(c=>c.closedAt>=state.shiftStart&&c.closedAt<=state.shiftEnd),openAll=state.calls.filter(c=>c.status!=='closed'),delays=calls.filter(c=>c.firstDeparture).map(c=>c.firstDeparture-c.at),average=delays.length?(delays.reduce((a,b)=>a+b,0)/delays.length).toFixed(1)+' min':'—';if(!state.logs.at(-1)?.message.includes('Fin de garde'))log('Fin de garde : bilan disponible.');modal(`<span class="eyebrow">08:00 · BILAN DE GARDE</span><h2>24 heures au CSP</h2><p>Objectifs d’arrivée : ${calls.filter(c=>objectiveResult(c).arrival===true).length} / ${calls.length} · Missions terminées dans le délai : ${calls.filter(c=>objectiveResult(c).finish===true).length} / ${calls.length}</p><div class="recapStats"><p><b>${calls.length}</b> interventions reçues</p><p><b>${closed.length}</b> terminées</p><p><b>${openAll.length}</b> encore en cours</p><p><b>${average}</b> délai moyen appel → départ</p></div><p>${['SUAP','INC','AVP','OD'].map(type=>`${type} : ${calls.filter(c=>c.type===type).length}`).join(' · ')}</p><p>${state.shiftNoDispatch} appel(s) sans départ. ${state.incoming.length} appel(s) encore en qualification. Les interventions et équipages sont conservés si vous continuez.</p><div class="actions"><button id="continueShift">Continuer la garde</button><button id="stopShift">Arrêter ici</button><button id="newShift">Recommencer une garde</button></div>`);$('continueShift').onclick=()=>{continueShift(state);$('modal').close();log('Prolongation de 24 heures. Moyens et interventions en cours conservés.');renderUI();setTimeout(showShiftBrief,0);};$('stopShift').onclick=()=>{$('modal').close();state.paused=true;renderUI();};$('newShift').onclick=()=>location.reload();}
function advanceSimulation(seconds){while(seconds>0&&!state.paused&&!state.ended){const dt=Math.min(.25,seconds);seconds-=dt;elapsed+=dt;tickShift(state,dt,onCall,()=>{sound('call');renderUI();},()=>log('Appel traité : renseignements ou absence de besoin de secours. Aucun départ.',true));tickEngines(dt);tickVolunteers(state,m=>log(m,true),volunteerTravel.ready);if(state.recallAt!==null&&state.minute>=state.recallAt){recallCrew(state,state.recallCount);state.recallAt=null;log(`${state.recallCount} SPV arrivés au CIS et disponibles.`,true);}if(state.ended)endShift();}}
const guardClock=simulationClock({paused:()=>state.paused||state.ended,advance:advanceSimulation});
if(window.setInterval)window.setInterval(()=>guardClock.tick(),250);
function animate(now){const cg=engines.find(e=>e.kind==='VLCG');if(cg){cg.officer??=captain(world,0,0,state.playerProfile);dressCaptain(cg.officer,state.playerProfile);setInterventionHelmet(cg.officer,cg.status==='scene'&&state.playerProfile.outfit==='station');cg.officer.rotation.set(0,0,0);cg.officer.children[1].rotation.x=0;cg.officer.children[2].rotation.x=0;cg.officer.visible=cg.status==='scene';cg.officer.position.set(cg.model.position.x+3,.3,cg.model.position.z+2);}requestAnimationFrame(animate);const wallDt=Math.max(0,(now-last)/1000),dt=Math.min(.06,wallDt);last=now;guardClock.tick(now);

 const hour=state.minute/60%24,night=(hour>=20||hour<7);if(night!==lastNight){lastNight=night;world.background.set(night?'#15272e':'#acbfbc');world.fog.color.copy(world.background);hemi.intensity=night?.5:2;sun.intensity=night?.3:3.2;sun.color.set(night?'#9bbdda':'#fff0d3');district.lamps.forEach(l=>l.material.emissiveIntensity=night?3:.15);$('night').textContent=night?'☀ Éclairage de jour':'☾ Éclairage de nuit';}
 radioVoice.sync(state.paused||state.ended||document.hidden);const soundingEngines=engines.filter(e=>e.siren||automaticSiren(e,roadVehicles(),sirenJunctions));const audibleEngines=soundingEngines;const sirenGain=Math.max(0,...audibleEngines.map(e=>vehicleSirenGain(e)*sirenScheduleScale(state.minute,e.sirenMode)));syncSiren(!document.hidden&&!state.paused&&!state.ended&&audibleEngines.length>0,sirenGain,vehicleSirenGain(selected())*sirenScheduleScale(state.minute,selected().sirenMode));extraFeatures?.visuals(elapsed,night);syncAmbience(extraFeatures?.ambient()||{},state.paused||state.ended);district.beach.update(elapsed,state.minute);volunteerTravel.lights(night);noria.visuals();for(const e of engines)updateStationRefillVisual(e);life.update(elapsed,state,engines);police.lights(elapsed,night);homeLife.update(state,engines.find(e=>e.kind==='VLCG'));responseFx.update(elapsed,state);foamVisuals.update(state.calls);extricationVisuals.update(state.calls,engines,hazards,victims,elapsed);updateSupplyCrew(elapsed);updateElevatorVictims();updateRescueVictims();updateNauticalVisuals(elapsed);for(const [id,record]of victims){const c=state.calls.find(x=>x.id===id);if(c&&!c.nautical&&!c.complication)record.model.visible=!!c.patients?.some(p=>!p.evacuated&&(!p.assignedTo||!(p.transportRequired!==false&&engines.find(e=>e.id===p.assignedTo)?.patientProgress>.5)));else if(c?.patients?.every(p=>p.evacuated||p.assignedTo&&engines.find(e=>e.id===p.assignedTo)?.patientProgress>.5))record.model.visible=false;}aerialVisuals.update(state,elapsed,victims);for(const [id,effect]of fireEffects){const c=state.calls.find(c=>c.id===id);effect.setActive(c?.status!=='closed'&&c?.progress<1&&(!c.inspection||c.fireConfirmed===true));effect.update(elapsed,(1-(c?.progress||0))*(1+(c?.spread||0)*.5),c?.spread||0);}for(const e of engines){e.model.visible=!e.mutualAid||!['ready','departing'].includes(e.status);e.model.userData.headlights.forEach(l=>l.material.emissiveIntensity=night&&!e.atResidence?4:0);const pulse=Math.sin(now*.018+(e.home[0]))>0;updateBlueLedEffects(e.model,e.beacons||e.model.userData.routineTesting,now,night);e.model.userData.penetrationLights?.forEach((lamp,i)=>{lamp.material.emissiveIntensity=e.beacons&&pulse===(i%2===0)?5:.03;});updateAmberEffects(e.model,e.amber??['scene','reconditioning'].includes(e.status),e.amberPattern||'alternate',now,night);const rotating=updateRotaryBeacons(e.model,e.beacons,elapsed,camera.position);e.model.userData.light.visible=!rotating&&e.beacons;e.model.userData.light.intensity=e.model.userData.light.visible&&pulse?(night?8:3):0;e.model.userData.ring.visible=false;}for(const v of district.citizens){if(v.model.visible===false)continue;const r=v.road,phase=(elapsed*.022+v.phase)%2,t=phase<1?phase:2-phase,dx=r.b[0]-r.a[0],dz=r.b[1]-r.a[1],len=Math.hypot(dx,dz);perimeters.movePedestrian(v,[r.a[0]+dx*t+dz/len*5.7,r.a[1]+dz*t-dx/len*5.7],state.paused||document.hidden?0:dt);v.model.children[1].rotation.x=v.waitingForPerimeter?0:Math.sin(elapsed*6+v.phase)*.4;v.model.children[2].rotation.x=-v.model.children[1].rotation.x;}for(const v of district.traffic)v.model.userData.headlights.forEach(l=>l.material.emissiveIntensity=night?3:0);for(const [id,h]of hazards){const c=state.calls.find(c=>c.id===id);if(c?.requires==='CCF')h.children.forEach((f,i)=>{f.scale.y=(.8+Math.sin(elapsed*8+i)*.3)*(1-c.progress);});}for(const [id,m]of markers){m.scale.setScalar(1+Math.sin(elapsed*3+id)*.15);}
 panKeyboard(dt);if(follow){const target=selected().model.position.clone();target.y=1;const delta=target.clone().sub(controls.target).multiplyScalar(Math.min(1,dt*3));controls.target.add(delta);camera.position.add(delta);}controls.update();renderer.render(world,camera);if(now-lastUI>600){renderUI();lastUI=now;}}
document.querySelector('.sceneCaption').innerHTML=`<span class="eyebrow">${block.name}</span><span>Centre-ville · commerces · forêt & lac · stade · rocade</span>`;state.selected='VLCG';setGarage(true);extraFeatures=installGuardFeatures({state,engines,world,district,camera,controls,renderer,modal,renderUI,selectIncident,selectEngine,engageUnits,returnEngine,onCall,log,callNow,navigationDistance});
if(resumeGuard){try{restoreGuard(resumeGuard,state,engines,district.hydrants,camera,controls,c=>onCall(c,true));}catch(error){state.paused=true;log('Reprise impossible : '+error.message,true);}}
log(resumeGuard?`Garde reprise à ${time(state.minute)} · simulation en pause.`:`Prise de garde à 08:00. ${playerLabel(state.playerProfile)}, vous dirigez le centre et pouvez vous rendre sur place avec votre VLCG.`);tickShift(state,0,onCall);renderUI();$('loading').remove();if(!resumeGuard&&/Chrome|Safari|Firefox|Edg/i.test(globalThis.navigator?.userAgent||''))showShiftBrief();requestAnimationFrame(animate);
if('serviceWorker' in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});

export {extricationVisuals,aerialVisuals,requestAerial,state,engines,homeLife,statusText,destinationText,applyPlayerProfile,showPlayerProfile,respond,engageUnits,tickEngines,selectEngine,selectIncident,returnEngine,controls,capability,cameraKeys,panKeyboard,camera,onCall,advanceSimulation,endShift};

export {noria,police,perimeters,aftermath,openPlacement,choosePlacement,tacticalOptions,placementMarkers,reactiveTraffic,advanceTraffic,volunteerTravel,roadVehicles,hazards,fireEffects,updateNauticalVisuals,waterBoats,vehicleObstacles,trafficControl,district,setRoute,advanceVehicle,moveTraffic};

export {recoverNonPlayers,configureStation,refreshStation,lakeIntake,toggleLakeSupply,ventilation,buildingActions,roadClearance,finishCall,navigationDistance,vehicleSirenGain,repositionEngine,toggleHydrant};

export {setMarkup,showShiftBrief};

export {extraFeatures};
